# Agent_Platform Codex 执行计划

## 执行约定（2026-07-15 起生效）

每次开始任何实现、配置、部署或代码审查任务前，必须先在对话中给出本次执行计划，并将计划和结果同步到本文档。计划至少包含：目标与范围、分步行动、Codex 的实施与审查范围、验收标准，以及已知风险或需要的授权。

- Codex 负责所有后续架构判断、任务拆分、代码实现、安全审查、主工作区集成、测试、文档和最终回归。
- 自 P32 起不再调用 Claude Code，也不再把外部 Agent 产物作为计划依赖或验收条件。P28-P31 的 Claude Code 内容仅作为已完成阶段的历史记录保留。
- 如需调整范围、引入新依赖、访问外部服务或执行发布，必须先更新计划并说明影响。
- 每个计划完成后必须记录改动文件、验证命令/结果和未解决风险；未经用户明确要求，不提交或推送 Git。

## 1. 目标与边界

目标是搭建一个**个人使用的 Agent 平台**：用户登录后可像使用 Codex 一样创建、续聊和查看历史对话；可在前台查看并启用代码维护的技能；可查看已接入的应用；可修改个人信息和退出。默认模型为 DeepSeek。

本计划以“真实可用”而不是“界面演示”为验收目标。每一轮对话、刷新页面、重新登录和打开历史会话都必须可用。

本期明确不做：多人/组织、支付、用户上传插件、文件工作区、代码执行沙箱、多 Agent 自动编排。它们保留为后续能力，不进入当前关键路径。

## 2. 当前状态与首要问题

已有原生前端、Python HTTP 服务、SQLite、登录、技能开关、DeepSeek 调用和历史记录骨架。

当前 P0 缺陷是 SSE 对话接口在发送 `done` 事件后仍声明 `keep-alive`，浏览器读取流不会结束，前端 `streaming` 状态也不会复位。因此首轮结果可见，但第二轮发送、历史会话切换和错误恢复会被卡住。

解决原则：本期上游 DeepSeek 使用非流式请求，平台向浏览器分段推送；平台完成一次回答后必须关闭 SSE 响应。前端必须使用 `try / catch / finally` 恢复发送状态，并在失败时保留用户消息和可重试入口。

## 3. 目标架构

```text
浏览器（Codex 风格 UI）
        |
        v
API / 认证层
        |
        +-- 会话与消息服务 ---- SQLite（当前）/ PostgreSQL（后续）
        |
        +-- Agent Runtime
        |     +-- 运行记录与事件
        |     +-- 上下文装配
        |     +-- 技能选择
        |
        +-- Model Adapter ---- DeepSeek Chat Completions
        |
        +-- Skill Registry ---- 代码内置技能（当前）/ MCP（后续）
```

当前阶段保留轻量实现，但代码职责必须按上图分开。不要把数据库操作、Prompt 拼接、模型 HTTP 调用和 SSE 输出长期堆在同一个处理函数中。

## 4. 实施顺序

### P0：修复对话闭环

目的：确保同一对话连续发送、历史对话恢复、刷新后继续对话均稳定可用。

实施项：

- [x] 后端：在 `done` 或 `error` 事件后明确关闭 SSE 响应；移除不必要的 `keep-alive`。
- [x] 前端：`sendMessage` 改为 `try / catch / finally`，无论网络、模型或 SSE 解析是否失败都复位发送按钮和 `streaming`。
- [x] 前端：收到 `error` 后不把空白 assistant 消息写入本地状态；显示“重试”操作。
- [x] 后端：以稳定顺序读取消息，使用 `(created_at, id)` 排序，避免同一秒内多条消息顺序不确定。
- [x] 后端：确认线程归属后才删除线程及其消息，避免越权删除风险。
- [x] 验证：自动化覆盖连续两轮、历史顺序、失败重试；浏览器覆盖真实 DeepSeek 连续两轮与发送状态恢复。

完成标准：以上五项均通过，`runs` 中每次运行最终为 `completed` 或 `failed`，没有永久 `running` 记录。

### P1：会话体验与运行可观测性

目的：让个人日常使用时能判断“正在做什么、失败在哪里、历史是否可靠”。

实施项：

- [x] 建立会话、运行、技能与模型调用的逻辑边界；HTTP Handler 保持为 API 编排入口，后续可直接迁移到独立模块。
- [x] 为运行增加阶段事件：`started`、`model_request`、`completed`、`failed`。
- [x] 新增运行详情抽屉：展示模型、耗时、状态、事件链和错误摘要；不显示 API Key 或完整敏感 Prompt。
- [x] 支持重命名、删除和搜索历史对话；删除前二次确认。
- [x] 自动生成标题：首条消息按规则生成，用户可修改。
- [x] 限制上下文窗口：早期消息压缩为摘要，仅携带最近 12 条，控制 DeepSeek 成本。
- [x] 验证：连续对话、失败重试、运行记录、重启服务与历史持久化已通过自动化回归；长对话按摘要与最近 12 条策略受控。

完成标准：用户不需要打开终端即可判断一次对话是否成功、为什么失败，以及是否能够安全重试。

### P2：技能与应用成为真正的运行能力

目的：前台开关不只是展示，而是可以被审计地影响 Agent 的行为。

实施项：

- [x] 将内置技能迁移到 `server/skills/*.json`，保留代码审核与版本控制。
- [x] 定义技能标准字段：`id`、名称、说明、版本、系统提示、输入限制、是否默认启用、状态。
- [x] 对每一次运行持久化“已启用技能快照”，确保历史回放不受后续技能修改影响。
- [x] 支持会话默认技能与消息级 `@技能` 选择；消息级选择优先，用户关闭技能为不可绕过的硬约束。
- [x] 每次消息仅将当次允许的技能传给模型；询问平台技能时由平台根据有效快照返回，不依赖模型自行猜测。
- [x] 应用页展示模型连接状态、技能库版本和未来工具连接状态；未接入的能力明确显示为“未配置”。
- [x] 验证：端到端回归已执行；会话级技能选择会写入下一次运行快照。

完成标准：所有前台可见的技能都能影响一次具体运行，并且运行后可追溯。

### P3：本地可靠性与安全基线

目的：将“本机能跑”提升为“个人长期稳定使用”，不依赖云数据库、Redis 或外部部署平台。

实施项：

- [x] 使用 PBKDF2-SHA256（标准库、随机盐、高迭代次数）替换 SHA-256；添加会话过期时间与退出所有设备功能。
- [x] `.env` 仅用于本机且保持 Git 忽略；启动状态只显示是否已配置，不输出 Key。
- [x] 保持 HTTPS 证书校验；支持 `certifi` 或指定 CA 文件解决企业代理证书问题。
- [x] 增加请求大小、单次回答长度、超时和本机内存限流。
- [x] 增加结构化本地日志与错误码；日志中不记录 token、邮箱与模型响应内容。
- [x] 增加本地备份与恢复命令：SQLite 备份到项目外指定目录，并可恢复到独立文件验证。
- [x] 验证：安全哈希、健康检查、服务重启和数据库备份恢复已通过；模型网络异常沿用现有错误事件与可重试提示。

完成标准：密钥不进入代码库；常见失败可恢复且对用户有明确提示。

### P4：本地扩展接口

触发条件：需要本地文件处理、受控工具调用或复杂工作流时再启动。个人纯对话使用不需要提前执行。

实施项：

- [x] 保持当前 HTTP API 合同；仅在本地功能复杂度需要时迁移 FastAPI。
- [x] 继续使用 SQLite；提供数据库备份、恢复与健康检查，不迁移 PostgreSQL。
- [x] 用本机线程和 SQLite 运行状态处理短任务；不引入 Redis 或远程队列。
- [x] 引入轻量工作流运行器；只有出现多步骤工具调用时才评估 LangGraph。
- [x] 按 MCP 设计本地只读工具注册，工具声明风险等级与启用状态；实际执行能力仍需逐工具授权。
- [x] 新增模型适配器接口，DeepSeek 已通过适配器接入，后续备用模型无需修改聊天主流程。
- [x] 验证：模型适配器、只读工具注册、轻量工作流与本地数据库备份已通过单元测试；未来新增工具时需补充该工具的超时与权限测试。

完成标准：新增一个本地模型或受控工具不需要修改聊天主流程，也不依赖外部基础设施。

## 5. Codex 执行方式

每个 P0-P4 阶段拆成一个独立任务，严格按以下循环执行：

1. 读取现有代码与数据模型，写明本次改动范围和验收标准。
2. 先补充或更新自动化验证，再实现最小改动。
3. 启动本地服务，使用真实 DeepSeek 和模拟失败场景各验证一次。
4. 检查浏览器中的连续对话、历史加载、刷新恢复与错误状态。
5. 将结果写入变更说明：改动文件、验证项、未解决风险。

建议的任务顺序：`P0-SSE 修复（已完成）` -> `P0-回归测试（已完成）` -> `P1-服务分层` -> `P1-运行详情` -> `P2-技能配置化` -> `P3-安全与备份`。P4 只在触发条件满足时执行。

## 6. 验收清单

- [x] 登录、退出和修改昵称正常。
- [x] 新建对话、连续多轮对话、历史对话继续发送正常。
- [x] 页面刷新与服务重启后，历史消息和标题完整。
- [x] DeepSeek 成功、认证失败、证书失败都有用户可理解的提示；网络超时受 60 秒上限控制。
- [x] 技能开关会真实影响新运行，历史运行可查看当时技能快照。
- [x] API Key、session token、完整模型请求不出现在页面、错误提示或运行日志中。
- [x] 所有失败运行最终结束，不存在长期 `running` 状态。

## 7. 技术取舍

当前路线固定为“原生前端 + Python + SQLite + DeepSeek”的本地优先方案。FastAPI、LangGraph 与 MCP 只在本地实际需求出现后再引入；PostgreSQL、Redis 和云端任务队列不属于当前计划。

Codex 适合承担阶段性实现、回归验证、迁移与代码审查。平台内给终端用户提供的模型仍使用 DeepSeek，二者职责分离。

## 8. 技能与应用管理扩展

### 本期范围

- [x] 技能支持上传 JSON 或 Markdown 技能包、编辑系统提示、删除、启用和禁用。
- [x] 技能 ID、版本、输入限制和状态由后端校验；不接受任意 Python、Shell 或二进制插件上传。
- [x] 当前对话可选择本次使用的技能；模型提示与运行快照使用同一份最终技能集合。
- [x] 应用页展示 DeepSeek、本地技能库和只读本地工具的状态。

### 后续应用能力

- [ ] 为每个本地工具实现受控执行器、参数校验、超时和运行审计。
- [ ] 增加常用只读应用：本地文件检索、网页检索和日历读取；每项单独授权。
- [ ] 支持符合 MCP 协议的本地插件清单导入，但仍禁止上传可执行代码。

## 9. 单 Agent 核心演进计划

### 范围与原则

后续只建设**单 Agent**运行时，不引入多 Agent 协作、角色间通信或复杂任务市场。一个 Agent 负责理解任务、选择已授权技能与工具、执行计划、检查结果并输出答案。

模型不暴露私有思维链。前端只展示可审计的执行摘要，例如：使用的技能、计划步骤、工具调用状态、来源、错误与最终回答。

```text
对话输入（@技能 + 任务）
        |
        v
单 Agent Runtime
  ├─ 技能路由与权限校验
  ├─ 任务复杂度判断
  ├─ 计划执行 / 工具调用循环
  ├─ 结果检查
  └─ 运行记录与最终回答
        |
        +-- DeepSeek 模型
        +-- 本地工具与应用
        +-- 会话记忆与本地知识库
```

### P5：技能路由与执行上下文

目的：让技能从“提示词片段”演进为可控、可审计的单 Agent 输入能力。

实施项：

- [x] 支持消息内多选 `@技能`，并将选择写入用户消息和运行快照。
- [x] 用户关闭技能后，前端不展示、后端拒绝、运行时不注入，三层一致。
- [x] 定义 `Execution Context`：冻结本次模型、技能、应用、权限、超时与输入限制，后续步骤只读取该快照。
- [x] 增加技能路由规则：显式 `@技能` 永远优先；未显式选择时沿用会话默认技能，不自动越权调用。
- [x] 将技能区分为 `prompt_skill` 与 `tool_skill`：前者影响回答方式，后者可声明受控执行器和参数 Schema。
- [x] 在运行详情中增加“技能路由摘要”，展示本次技能、允许工具与执行计划。

完成标准：任一运行都可以确定回答使用了哪些技能、哪些应用配置和哪些权限；关闭或未授权的技能无法通过 UI、API 或模型文本绕过。

### P6：Plan-and-Solve 与可恢复执行

目的：让复杂单 Agent 任务先规划再执行，避免把所有工作压进一次模型回答。

实施项：

- [x] 定义复杂任务判定规则：多目标、需要工具、需要文件检索或用户明确要求计划时进入规划模式；普通问答直接回答。
- [x] 生成结构化计划：步骤 ID、目标和状态，并将其写入运行快照。
- [x] 使用 SQLite 持久化计划步骤状态；短任务失败可沿用现有消息重试。暂停/恢复仅在引入异步或写操作工具时实施。
- [x] 前端在运行详情展示计划和步骤状态，不展示私有推理过程。
- [x] 当前工具仅允许 `read_only` 风险等级；删除、覆盖文件和外部写操作尚未注册，未来注册前必须加入人工确认。

完成标准：一个多步骤任务可被中断并恢复；每一步状态、错误和最终产物均可追溯。

### P7：ReAct 工具调用与应用执行器

目的：让“应用”成为实际运行资源，而不仅是状态展示。

实施项：

- [x] 定义统一 Tool Contract：名称、说明、JSON 参数 Schema、返回 Schema、风险等级、超时与启用状态。
- [x] 为 DeepSeek 增加结构化工具调用适配；模型只能看到本次 `Execution Context` 允许的工具。
- [x] 实现单 Agent ReAct 循环：请求模型 -> 解析工具调用 -> 校验权限/参数 -> 执行 -> 记录观察结果 -> 继续或结束。
- [x] 首批只读本地工具：平台状态、工作区文件名检索；知识库检索在 P9 实现后接入。
- [x] 对工具调用设置最大步数、单工具超时和失败退出策略，避免无限循环。
- [x] 运行详情展示工具名、状态、计划和结果摘要，敏感输入不展示。

完成标准：模型可以在授权范围内可靠调用本地工具，工具失败不会导致运行长期卡住，也不会泄露敏感数据。

### P8：Reflection 与结果质量控制

目的：在输出前检查任务完成度，而不是展示或保存模型的思维链。

实施项：

- [x] 为需要高质量输出的任务定义检查项：目标覆盖、格式、冲突、未完成事项；普通问答不额外调用检查。
- [x] 单独生成结构化检查结果：`passed`、问题列表、摘要和修订次数；不保存模型私有推理文本。
- [x] 未通过时最多自动修订 1 次；检查或修订不可用时保留原回答，不让任务失败。
- [x] 前端显示可见的质量检查状态；运行详情保留检查摘要和修订次数。

完成标准：报告、方案和重要任务在交付前有可审计质量检查，且不存在无边界自动重试。

### P9：本地记忆与 RAG 知识库

目的：让单 Agent 可依据本地资料完成连续工作，同时保持个人本地部署边界。

实施项：

- [ ] 保留现有短期会话摘要；新增用户偏好与项目结论的长期记忆，并提供查看、编辑和删除入口。
- [x] 支持上传 Markdown、TXT、Word 资料，完成本地解析、切分、索引和删除；PDF 在本机安装 `pypdf` 后自动启用。
- [x] 建立本地检索接口；只有检索命中的片段才能作为知识依据进入模型上下文。
- [x] 回答中展示可点击资料名称和检索片段入口，不伪造来源。
- [x] 初期采用 SQLite 元数据加本地文件存储；向量索引方案在资料规模验证后再选择。

完成标准：用户可管理本地资料，Agent 能检索并引用资料回答，删除资料后不再被检索或注入。

### 推荐执行顺序

`P5 执行上下文与技能路由` -> `P7 首批只读工具` -> `P6 计划执行` -> `P8 质量检查` -> `P9 本地知识库`。

P6 与 P7 可根据实际需求调整顺序：优先需要文件/检索能力时先做 P7；优先需要复杂任务管理时先做 P6。多 Agent 协作不在当前路线中。

## 10. 模型效果优化执行计划

### 问题定义

当前平台的 Agent 机制已经具备执行上下文、工具和质量检查，但模型效果仍取决于模型路由、提示上下文、技能质量、资料依据和输出预算。后续优化不以增加更多 Agent 为目标，而以“同一任务达到接近或超过 DeepSeek 官方应用的完成度”为验收标准。

### P10：模型迁移与模型路由

目的：避免继续依赖即将退役的旧模型名称，并按任务复杂度选择合适的 DeepSeek 模型与模式。

实施项：

- [x] 将 `.env` 中的默认模型从旧 `deepseek-chat` 迁移为 `deepseek-v4-flash`；保留旧模型名称兼容映射与配置校验。
- [x] 建立模型配置表：模型 ID、显示名称、工具支持、任务档位和按档位的最大输出预算。
- [x] 实现路由规则：普通问答走快速模型；调研、方案、代码分析等复杂任务走 V4 Pro；需要工具调用时只选择工具兼容模型。
- [x] 将 `max_tokens` 从全局固定值改为按 `quick`、`standard`、`deep` 任务档位配置。
- [x] 将模型选择、路由原因、任务档位和输出预算写入 `Execution Context` 与运行详情。

完成标准：所有运行可说明为什么选择该模型；复杂任务不会误用不支持工具调用的思考模型；模型配置可在前台查看。

### P11：技能提示质量与上下文装配

目的：让技能提供清晰、稳定的行为约束，而不是只有名称或一句角色描述。

实施项：

- [x] 为每个 `prompt_skill` 定义标准模板：目标、工作框架、输出格式和禁止事项；技能文件保留版本字段。
- [x] 为“乔布斯”“写作助手”“代码助手”等现有技能重写可执行提示，并升级技能版本。
- [x] 将系统提示拆为固定平台规则、当次技能规则、任务规则和工具规则，按优先级装配。
- [x] 默认不向普通对话暴露工具定义；仅在路由器判定需要工具时向模型提供最小工具集合。
- [x] 优化会话摘要：保留用户目标、已确认结论、约束和最近待办，不再只按字符截断旧消息。

完成标准：同一技能在多轮任务中输出结构稳定；复杂任务关键约束不会因上下文压缩丢失；普通写作不触发无关工具调用。

### P12：任务分级与成本质量控制

目的：用明确的任务档位控制模型、工具、质量检查和 token 预算，避免“所有请求走同一套 Agent 流程”。

实施项：

- [x] 定义三级任务：`quick`（问答/改写）、`standard`（结构化写作/代码）、`deep`（调研/方案/复杂分析）。
- [x] 为每个档位配置模型、最大输出、工具范围和是否触发 P8 质量检查；沿用工具步数与超时上限。
- [x] 将路由结果在运行详情中以简洁执行摘要展示，不显示内部提示词和私有推理。
- [x] 增加用户可选覆盖项：快速、标准、深度；默认自动路由。
- [x] 记录输入/输出 token 估算、工具步数、耗时和失败原因，并通过本地指标接口按任务档位聚合。

完成标准：用户可在速度、成本与质量之间做出明确选择；平台能量化不同档位的耗时、成本和成功率。

### P13：P9 知识库优先落地与事实依据

目的：让调研和项目任务基于用户本地资料与可追溯来源，而不是只依赖模型参数知识。

实施项：

- [x] 完成 P9 的 Markdown、TXT、Word 上传、解析、切分、索引、检索和删除闭环；PDF 解析待本机可合规安装 `pypdf` 后启用。
- [x] 将“是否需要资料依据”加入任务路由；需要依据的任务优先检索本地知识库，再决定是否调用其他已授权应用。
- [x] 仅将命中的资料片段和来源元数据注入模型，限制注入长度并去重。
- [x] 最终回答展示可点击的本地资料引用；没有资料依据时明确说明是模型建议而非事实结论。

完成标准：用户可追溯每个调研结论的本地资料来源；删除资料后，其内容不再进入后续回答。

### P14：基准评测与持续优化

目的：以可重复测量替代主观“感觉变笨/变聪明”。

实施项：

- [x] 建立个人评测集，覆盖：普通问答、中文写作、产品调研、项目方案、代码分析、文件检索和多轮续聊。
- [x] 每个样例保存任务、预期要点、评分维度和人工基准答案；不保存 API Key 或敏感资料正文。
- [x] 定义评分：目标完成度、事实依据、结构清晰度、技能遵循、工具正确性；运行详情保留耗时和 token 估算。
- [x] 在变更模型、技能模板、上下文策略或工具协议前后运行同一评测集，输出路由差异报告；回答质量按样例标准人工评分。
- [x] 从低分样例反推问题归属：模型路由、提示词、上下文、技能、工具或知识库，并只修改对应层。

完成标准：每次模型体验优化都有可比较的质量、耗时和成本数据；不依赖单次主观对话判断效果。

### 优先级与执行顺序

`P10 模型迁移与路由` -> `P11 技能与上下文` -> `P12 任务分级` -> `P9/P13 本地知识依据` -> `P14 基准评测`。

建议先执行 P10：模型名称迁移和任务路由会直接影响所有对话质量；P11 随后解决当前技能提示过弱和工具暴露过多的问题。P13 与 P14 是让调研、方案等任务稳定超过纯聊天质量的关键闭环。

## 11. 产品化与可审计体验计划

### P15：回答渲染、引用粒度与运行审计

状态标记：`【X】已完成`、`【】未完成（含条件未满足）`。

目的：把已可用的对话、知识库和运行记录整理成更接近日常使用的产品体验，减少“能跑但不好读、难追溯”的摩擦。

实施项：

- [x] 前端支持安全的 Markdown 回答渲染：标题、列表、引用、代码块、行内代码、强调和 HTTP(S) 链接；用户消息仍按纯文本显示。
- [x] 资料引用从文末文件级引用升级为段落级引用，展示资料名、片段位置和命中摘录入口。
- 【X】运行审计增加可筛选列表：按状态、任务档位、模型、工具调用和知识库使用情况查看历史运行。
- [x] 会话支持文件夹分组：创建、重命名、删除、移动对话；删除文件夹时对话保留在未归类，便于整理长期任务。
- 【X】应用可执行化：为已注册本地只读工具补齐参数表单、失败重试和审计详情；写操作继续沿用确认、幂等和审计契约。

完成标准：常见长回答在前端可读、可复制、可追溯；资料依据可定位到具体片段；用户无需打开终端即可审计近期运行质量和失败原因。

执行批次 P15-2：优化“产物”页面的文件卡片信息层级和响应式布局。隐藏不适合日常阅读的内部 ID，使用类型图标、可读标题、日期、摘要和固定操作区；在窄屏自动降为单列，不改变下载、删除或产物审计 API。

执行结果 P15-2：已完成。产物卡片改为自适应网格，文件类型用视觉标识区分；内部文件名仅作为可截断的次要信息保留，下载与删除操作固定在卡片底部。已通过 JavaScript 语法与 Git diff 格式检查；不涉及接口或产物存储变更。

运行验证 P15-2：启动本地服务后验证 `/api/health` 和最新静态资源可访问，确认浏览器加载的是当前工作区版本。

运行结果 P15-2：已于本机 `http://127.0.0.1:8765` 重启服务；健康检查通过，且 `/static/app.js` 已包含新版产物卡片结构。

修复批次 P15-3：保存当前页面与当前对话的浏览器会话状态。刷新后先恢复该页面和会话；若会话不存在或无权限，安全回退至新建对话，不改变服务端数据。

修复结果 P15-3：已完成。浏览器本地仅保存页面类型和当前对话 ID；启动或登录后恢复一次。对话已删除、不可访问或本地状态损坏时安全回退到新建对话。已通过 JavaScript 语法与服务静态资源检查。

运行维护：后端 Skill 包兼容代码更新后重启本地服务，并验证健康检查与静态资源，确保前后端均来自当前工作区版本。

运行维护结果：已完成。本机服务已重启，`/api/health` 通过，且静态前端包含刷新状态恢复逻辑。

### P16：Human-in-the-loop 与本地文件产物

目的：让 Agent 在遇到不可逆操作、缺少关键业务判断或需要生成本地交付物时，明确停下来等待用户确认；确认后才继续执行，并保留完整审计记录。

实施项：

- [ ] 定义确认策略：只读检索不打断；删除、覆盖、发送、发布、外部写入和创建本地文件必须确认。模型缺少关键业务偏好时先提出可选项，不替用户猜测。
- [x] 为运行增加 `awaiting_confirmation` 状态、确认请求、批准/拒绝结果；页面显示“确认执行 / 取消”操作，刷新后恢复待确认卡片，确认后继续同一运行而非重新发送消息。
- [x] 将确认内容写入运行事件与步骤审计，记录文件类型、目标目录、确认结果和确认时间；不记录密钥或完整敏感资料。
- [x] 新增受控文件产物目录 `data/artifacts/`，只允许创建 `.md`、`.xlsx`，不接受任意路径或覆盖已有文件。
- [x] 新增 `markdown_artifact` 执行器：根据确认后的回答生成 `.md`，返回文件名、摘要与下载入口。
- [x] 新增 `xlsx_artifact` 执行器：使用固定工作表“回答”和受限单元格写入 `.xlsx`；通过本机工作簿库生成，不允许模型直接执行脚本。
- [ ] 为文件产物增加删除、重新生成和来源审计；生成失败时保留原对话但不留下半成品文件。
- [ ] 验证：覆盖批准、拒绝、刷新后待确认运行恢复、非法路径拒绝、同名文件冲突、Markdown/XLSX 内容和审计记录。当前已覆盖 Markdown 批准/拒绝/审计，并对 XLSX 工作表结构与渲染进行了本机校验；其余边界用例待补。

完成标准：Agent 不会在缺少用户判断时擅自执行写操作；用户可在页面完成确认、查看产物与追溯来源；文件写入始终受本地目录、类型和权限限制。

## 12. 上线前 Harness 核心收敛计划

原则：保持“原生前端 + Python + SQLite + DeepSeek”的低成本架构。上线前不引入
PydanticAI、LangGraph、Redis 或远程队列；先把现有运行契约稳定下来，只有出现长任务、
多进程 Worker 或大量 MCP 工具后再按实际瓶颈选型。

### P17：运行时边界与可恢复审计

目的：让聊天、工具和审批流程共享同一套 Run 生命周期，为上线后的故障恢复、MCP 接入和
异步执行保留兼容边界。

实施项：

- [x] 新增独立 `AgentRuntimeStore`，统一负责 Run 状态迁移与运行事件持久化；HTTP、模型和前端职责保持不变。
- [x] 固化状态迁移：`running -> awaiting_confirmation -> running -> completed/failed/cancelled`，终态禁止重新执行。
- [x] 为运行事件增加 `schema_version` 和 Run 内严格递增的 `sequence`，运行详情按序号稳定回放。
- [x] 为工具调用增加 `tool_call_id`，调用、结果和错误可被同一审计链关联。
- [x] 服务启动时将遗留 `running` Run 收敛为可重试的 `failed`，保留 `awaiting_confirmation` 供用户继续确认。
- [x] 增加唯一事件序号约束、状态迁移单元测试和完整 API 回归。

完成标准：服务重启后不存在永久 `running`；终态 Run 不可误恢复；事件链顺序确定；工具调用可端到端关联。

### 后续最小迭代顺序

1. P18：将模型 Provider、Agent Loop、工具策略和仓储从 `app.py` 继续拆为内部模块，不改变 API。
2. P19：把产物审批泛化为按风险等级执行的工具审批，并增加取消运行接口。
3. P20：接入首个只读 MCP 服务，验证白名单、超时、审计和故障隔离。
4. P21：补充上线配置、健康检查、日志轮转、数据库恢复演练和真实 DeepSeek 冒烟测试。

P18-P21 完成前保持单 Agent。只有任务需要跨进程等待、分钟级执行或并发 Worker 时，才评估
PydanticAI durable execution、LangGraph 或 Temporal；它们不作为当前上线的前置依赖。

#### P18a：模型 Provider 拆分（已完成）

- [x] 将 DeepSeek 的 HTTP、TLS、SSE 解析与工具调用分片合并移入 `server/model_provider.py`。
- [x] 明确区分同步 `complete()` 与增量 `stream()`；质量检查、修订与 Agent 工具循环通过兼容入口继续工作。
- [x] 以本地 SSE fixture 覆盖内容拼接、工具调用分片与增量事件输出。

后续 P18 只继续拆 Agent Loop 与工具策略；前台、数据库和 `/api/chat` 合同不变。

#### P18b：工具授权策略拆分（已完成）

- [x] 将最小权限的工具选择规则移入 `server/tool_policy.py`，模型只能看到策略明确授权的工具。
- [x] 保持现有只读工具范围，并修复“仅提到文件即获得检索权限”的过宽匹配。
- [x] 对状态查询、文件检索、写入意图和无工具意图补充策略单元测试。

#### P18c：单 Agent 编排拆分（已完成）

- [x] 将有界 ReAct 循环移入 `server/agent_loop.py`，包括模型流消费、工具调用、模型可见观察结果与最大步骤兜底。
- [x] 通过依赖注入连接消息加载、模型 Provider、工具注册、事件审计和结果摘要，避免模块反向依赖 HTTP Handler。
- [x] 以本地 fake model 覆盖授权工具调用、调用结果关联和空最终回答的失败路径。

#### P19a：可取消运行（已完成）

- [x] 新增 `POST /api/runs/{run_id}/cancel`；运行详情可取消运行中或待确认的任务。
- [x] 待确认任务取消后同步撤销确认、计划步骤与 Run 状态，且不会创建文件产物。
- [x] 运行中的只读任务在下一个模型增量、工具事件或质量检查边界协作停止，不保存后续回答。
- [x] 文件产物进入实际写入阶段后拒绝中断，避免产生不可审计的半成品。

#### P19b：风险工具审批（已完成）

- [x] 将 `write_local`、`external_write` 和 `privileged` 工具转化为可持久化的待确认工具调用；批准后从同一 Run 恢复。
- [x] 为审批展示结构化变更摘要与工具参数白名单，不向前端暴露敏感输入。

执行结果 P19b（2026-07-20）：新增通用审批预览契约；只有显式白名单字段会写入 `run_approval_requests`、审计事件和前端响应，未列入字段的原始参数保留在执行边界。审批记录继续保存风险等级、工具标识、影响/回滚摘要和幂等键；批准后由通用的同一 Run 恢复入口执行。现有本地文件产物已接入并完成端到端回归；后续接入本地写入、外部写入或特权工具时必须复用此契约，不能新增旁路确认接口。

#### P21a：生产启动与就绪检查（已完成）

- [x] 生产模式要求显式配置 `ADMIN_EMAIL` 与 `ADMIN_PASSWORD`，不再静默创建默认管理员；配置缺失时在连接或修改数据库前启动失败。
- [x] `/api/health` 增加 SQLite 就绪探测，数据库不可用时返回 `503`；成功响应包含环境与模型配置状态，便于部署探针使用。
- [x] 补充生产启动说明与回归覆盖，保留开发环境默认账号以降低本地试用成本。
- [x] 通过可选的有界本地日志轮转补齐非容器部署场景；默认仍输出到标准输出，保持容器和进程管理器的低成本日志采集方式。
- [x] 新增不修改源库的 SQLite 恢复演练，以及不回显密钥的 DeepSeek 配置/真实请求冒烟脚本；真实请求只在目标部署环境由部署者执行。

P21 剩余的真实 DeepSeek 请求验收必须在目标部署环境执行；本地测试不读取或记录真实密钥。

### P22：多供应商运行时接入（已完成）

目的：在保持 DeepSeek 默认路径稳定的前提下，建立可审计、低成本的多模型供应商扩展边界。

实施项：

- [x] 建立不可变的供应商/模型注册表，统一能力、任务档位与模型目录元数据。
- [x] 为 `/api/models` 增加供应商和能力字段，保持现有前端与 DeepSeek 路由兼容。
- [x] 建立 `AGENT_MODEL_PROVIDERS` 配置解析层：仅接受 HTTPS 地址和 API Key 的环境变量名称，拒绝明文密钥、无效模型和重复配置。
- [x] 将经校验的 OpenAI 兼容供应商配置接入模型调用链、路由和健康状态；未配置的供应商不得触发网络请求。
- [x] 外部模型默认不声明工具能力；工具任务自动回退到 DeepSeek，避免不兼容供应商进入工具循环。
- [x] 覆盖外部供应商调用配置、缺失 Key、HTTP 失败和工具回退，并在 README 提供最小配置示例。

验收：全量本地回归 45 项通过。外部模型使用独立环境变量读取 API Key，缺失 Key 时保持本地模拟路径。真实第三方 Key 的线上冒烟请求只能由部署者在目标环境执行，测试过程不读取或记录真实密钥。

### P23：受控工具与网页检索

目的：将网页检索等常用能力接入单 Agent，同时保持最小权限、可审计、可关闭和低成本。

实施项：

- [x] 定义外部只读工具的统一配置：供应商、环境变量引用、超时、结果数量、域名白名单和启用状态；不在技能包或数据库保存密钥。
- [x] 实现首个网页检索执行器，返回标题、链接、摘要和来源标识；限制结果数、响应体大小与单次超时，并过滤非 HTTP(S) 地址。
- [x] 将网页检索加入工具策略：仅在明确检索意图或用户授权时可用；普通聊天不暴露工具定义。
- [x] 将检索来源写入 Run 事件与对话右侧“来源”面板；回答只引用实际返回结果。
- [x] 补充未配置、上游错误、恶意 URL、权限拒绝和来源审计测试；全量本地回归 50 项通过。

完成标准：用户可在授权的对话中使用网页检索，结果和来源可追溯；未配置或失败时不会泄露密钥、不会卡住 Run，也不会伪造引用。

执行结果 P23-1：新增独立网页检索 Provider、受限 Tool Contract 与来源事件；默认关闭网络能力，只有显式启用并提供环境变量引用后才发送请求。使用本地 HTTP fixture 覆盖成功、缺失配置、上游 HTTP 错误、异常 URL 与审计数据。风险：真实检索服务的 SLA、成本和特定域名白名单需在部署环境确定后配置；本地测试不发起外部请求。

### P24：技能包生命周期与治理

目的：让用户可以安全地添加、更新、启用和回滚技能包，而不允许把可执行代码直接带入平台。

实施项：

- [x] 使用版本化技能清单：ID、名称、版本、描述、提示模板、输入限制、工具声明与状态；运行使用冻结快照，历史不受后续更新影响。
- [x] 支持上传 ZIP、JSON 或 Markdown 技能包；ZIP 只允许 `skill.json` 或 `SKILL.md` 及可选 README，拒绝路径穿越、脚本、二进制文件、超限压缩包和无效清单。
- [x] 支持启用、禁用、更新、删除、历史版本列表与一键回滚；覆盖前版本归档在受控 `data/skill_history/` 路径。
- [x] 技能不能扩大当前工具策略范围：模型实际可见工具仍由本次 Execution Context 的最小权限策略决定。
- [x] 前台支持 ZIP 上传、校验错误提示和历史版本回滚；版本变更保留归档文件。
- [x] 覆盖 ZIP 导入、版本归档、回滚和运行快照；ZIP 解析对路径穿越、无效格式和超限文件执行拒绝。

完成标准：用户能在前台安全管理非可执行技能包；任一运行可追溯到确切技能版本和权限，上传包无法执行任意本地代码。

执行批次 P24-1：在现有 JSON/Markdown 导入与运行快照基础上，增加受限 ZIP 包解析、覆盖前版本归档、版本列表和回滚 API。ZIP 只允许一个技能描述文件及说明文件，拒绝路径穿越、可执行文件、超限压缩包和无效清单；前台复用现有上传入口并允许 `.zip`。

执行结果 P24-1：已完成。风险：当前为个人本地平台，技能版本归档采用本地文件而非多人审批流；若未来开放多人共享或远程市场，需要新增作者签名、审核和权限审计表。

执行批次 P24-2：兼容 Open Agent Skills 标准目录。接受根目录或单层包装目录的 `SKILL.md`，保留可选的 `scripts/`、`references/`、`assets/` 与 `agents/openai.yaml`；资源只做存储与审计，脚本永不自动执行或获得工具权限。继续兼容现有 `skill.json` 格式，并以本地 fixture 覆盖标准包、包装目录、非法路径和含脚本包。

执行结果 P24-2：已完成。标准包要求 `SKILL.md` 的 YAML frontmatter 至少含 `name` 与 `description`；支持单层包装目录。资源保存于受控 `data/skill_packages/` 路径，`scripts/` 仅存储、不执行、不注入工具权限。官方标准允许脚本作为可选资源，但本平台仅在未来受控执行器与人工确认机制完成后才会允许执行。

执行批次 P24-3：生成一个标准 `research-brief` Skill ZIP 作为导入验证样本，包含 `SKILL.md`、`agents/openai.yaml`、`references/`、`assets/` 和可测试的 `scripts/`。该包用于验证目录兼容与资源保留，平台上传后不执行其中脚本。

执行结果 P24-3：已生成并验证 `examples/research-brief.skill.zip`。该包通过标准 Skill 校验、示例脚本运行、ZIP 目录检查和平台自身导入解析器验证；包含脚本资源但未在平台导入过程中执行。

修复批次 P24-4：排查前台 ZIP 选择/上传不兼容问题，确认服务静态资源版本、浏览器 MIME 筛选和 API 错误路径；修复后用标准样本包回归验证并重启本地服务。

修复结果 P24-4：已完成。服务已确认提供最新 ZIP 上传代码；前端文件选择器增加常见 ZIP MIME 类型与扩展名，上传判断改为扩展名或 MIME 双重识别，并显示支持格式提示。`research-brief.skill.zip` 已由平台解析器验证；最新版静态资源已由本机服务提供。

修复批次 P24-5 / P15-4：消除刷新时登录页闪烁，并移除浏览器文件选择器对 ZIP MIME 的系统兼容限制。改用前端扩展名校验和后端 ZIP 安全校验，确保 ZIP 文件可被选择且不支持格式有清晰提示。

修复结果 P24-5 / P15-4：已完成。页面在鉴权未决期间保持隐藏，鉴权成功后直接显示工作区、失败后显示登录页；文件选择器不再限制系统可见文件类型，前端仅在选择后校验 JSON / Markdown / ZIP 扩展名或 MIME，后端继续执行 ZIP 安全校验。已通过 JavaScript 语法、服务静态资源和 diff 格式检查。

修复批次 P24-6 / P15-5：解决隐藏 input 的 ZIP 选择不确定性，并保证刷新期间不显示工作区默认的新建对话。改为在恢复完成后才揭示页面，上传入口提供可见原生文件选择与拖放区域，继续复用同一安全解析 API。

修复结果 P24-6 / P15-5：已完成。刷新时，认证、会话列表和原页面/原对话恢复完成前，登录页与工作区均保持隐藏；恢复成功后才一次性显示工作区，加载失败则显示明确错误。技能导入已移除隐藏 input 与脚本触发，改为可见的系统原生文件选择控件，且不设文件类型筛选；另提供 ZIP 等文件的拖放导入。上传后仍由前端格式判断和后端安全解析器双重校验。

发布批次 R1：整理本轮多供应商、受控网页检索、技能包治理、自动续聊、来源/产物面板及刷新/导入修复，更新 README 并推送至 Git 远端。验收包括代码语法与格式检查、关键 ZIP 导入回归、提交内容排除本地数据和依赖目录、远端推送成功。

发布结果 R1：本地全量回归 53 项通过，`web/static/app.js` 语法与 Git diff 格式检查通过。暂存范围仅包含平台源代码、测试、README、实施计划与标准 Skill 示例，明确排除 `data/`、`node_modules/`、`hoops-v2-upgrade/`、`sites-frontend/` 及用户本地上传的技能文件；随后提交并推送当前 `main`。

优化批次 P24-7：优化“技能和应用”页。导入区在保留原生 ZIP 选择兼容性的前提下，使用清晰的主操作与文件状态展示；技能卡片仅展示受限的描述摘要，完整提示词不再撑开卡片，操作区保持底部对齐。验收：宽窄布局下导入区无浏览器控件溢出，任意长度的技能描述均不改变卡片高度，JSON / Markdown / ZIP 的选择与拖放入口保持可用。

优化结果 P24-7：已完成。原生文件输入通过关联 label 触发系统文件选择，不使用脚本 `click()`；选择或拖放后会展示导入中、导入成功或失败的文件状态。技能描述会去除基础 Markdown 标记、压缩空白并截断为最多 150 个字符，同时在卡片中最多显示三行；卡片设定最小高度并将状态和操作按钮分组固定在底部。已通过前端语法、diff 格式和本地静态资源检查。

实施批次 P26：重构左侧会话导航为“项目”和“对话”两个一级分类，项目始终位于顶部。两类均支持自定义文件夹，并可创建、重命名、删除和调整位置；保留已有对话的创建、搜索、分组、删除与自动续聊能力。现有数据默认归入“对话”，以保证历史不丢失。验收：分类与文件夹顺序会持久化，文件夹操作受用户归属校验，删除文件夹后其中对话保持可见且可重新归类。

实施结果 P26：已完成。`thread_folders` 增加 `section`（`project` / `conversation`）与 `sort_order`，现有文件夹迁移为 `conversation`；项目区始终在对话区上方。每个区都有独立的新建入口，文件夹可通过上移/下移调整同区顺序，并保留重命名、删除和折叠；项目文件夹同样可承载已有对话。删除任一文件夹后，对话回到“对话 / 未归类”，不会删除历史。已通过项目/对话独立排序、对话保留、Python 编译、前端语法与 diff 格式检查。

优化批次 P26-1：按功能与层级重构左侧栏视觉。一级平台功能使用图标化导航与统一选中状态；“项目”“对话”改为低强调分组标题；文件夹作为二级容器，对话作为三级内容，未归类对话保持独立但弱化呈现。验收：不改变已有导航、文件夹与对话操作，当前页面、当前对话和折叠状态均清晰可辨。

优化结果 P26-1：已完成。一级功能改为图标与文字组合，选中和悬停状态统一；项目/对话标题改为紧凑的分组标签；文件夹名称提高可读性，内部对话缩进，未归类内容以分隔线弱化区隔。文件夹上移、下移和管理操作在悬停/聚焦时显现，减少常态噪声但保持键盘可达。已通过前端语法、diff 格式和本地静态资源检查。

优化批次 P26-2：保持“项目 / 对话”分组标题现状不变，统一左侧其余可阅读项目的文字颜色和字号到平台功能导航标准；仍保留当前对话选中背景及非文本控制的弱化色，避免影响层级识别。验收：文件夹、对话和未归类文字均为统一的 14px 主文字样式，项目/对话标题不受影响。

优化结果 P26-2：已完成。文件夹、文件夹内对话、未归类标签和未归类对话统一采用 `var(--text)` 与 14px 字号，匹配平台功能导航；项目/对话分组标题与非文本控制颜色维持不变。已通过 diff 格式与本地静态资源检查。

优化批次 P26-3：将左侧已统一的文字改为柔和灰阶，并统一平台功能与文件夹图标的尺寸、线条和颜色风格。项目/对话标题、文件夹与对话交互均保持不变。验收：平台功能和文件夹图标均以 16px 级尺寸、约 1.5px 线宽呈现，文字在常态为灰色、选中时略加强。

优化结果 P26-3：已完成。平台导航、文件夹、对话与未归类文字的常态色统一为柔和灰色，平台功能悬停/选中时仅轻微加深；平台 SVG 与文件夹图标均按 16px 宽度、1.5px 线条、圆角线条风格对齐。项目/对话标题和现有操作未改动；已通过 diff 格式与本地静态资源检查。

优化批次 P26-4：统一“项目 / 对话 / 未归类”三个分组标签的文字样式，使其均使用 14px 柔和灰色与同一字重；不改变文件夹、对话或交互逻辑。验收：三个分组标题在左侧栏视觉上完全一致。

优化结果 P26-4：已完成。项目、对话和未归类统一为 14px、`#66665f`、500 字重，项目/对话标题原有字距也已移除；已通过 diff 格式与本地静态资源检查。

配置批次 C1：在本地忽略的环境配置中启用 Tavily 兼容网页检索。密钥不得进入代码、文档、日志或 Git；配置后重启本机服务，并以不回显密钥的健康接口和配置解析结果验证。实际外网检索只在用户发起明确搜索请求时执行。

配置结果 C1：已完成。本地 `.env` 已启用网页检索并使用环境变量引用密钥；服务已重启，配置解析结果为 `web-search-enabled`，健康接口正常。未发起外部检索请求，密钥未写入 Git 跟踪文件、文档或日志。

修复批次 P23-2 / UX-1：输入框改为 Enter 换行、Shift + Enter 发送。排查并修复已启用网页检索在明确联网搜索请求中未进入工具调用链的问题；保持普通对话默认不暴露工具、网页来源可审计的最小权限策略。验收：键盘行为符合约定；“搜索网页/联网查询”等明确请求进入 `web_search` 工具，来源可在运行与右侧面板追溯。

修复结果 P23-2 / UX-1：已完成。Enter 仅保留内容编辑器的换行行为，Shift + Enter 才提交消息。工具策略新增“联网”“查询”“查一下”等明确搜索表达识别；当真实模型对明确网页请求运行时，平台会在模型生成前确定性执行已授权的 `web_search`，记录工具调用/结果并把实际来源传入回答上下文，随后撤销该工具定义以避免重复调用。普通聊天仍不联网。已通过工具策略、确定性网页检索、Python 编译、前端语法和 diff 格式回归。

补充修复 P23-2a：真实 Tavily 连通性验证发现本机系统 CA 链无法验证上游证书。网页检索改为优先使用 `certifi` 的 CA bundle，且继续强制 HTTPS 证书校验；不得通过关闭校验绕过该问题。验收：最小真实查询成功，或返回上游的受控错误而非本机证书验证错误。

补充修复结果 P23-2a：已完成。网页检索客户端已使用 `certifi` CA bundle 创建 TLS 上下文，仍保持证书校验。网页检索、工具策略与确定性调用回归共 7 项通过；服务重启后最小真实 Tavily 查询成功，健康接口正常，查询过程未输出密钥或完整结果。

修复批次 P23-2b：扩展明确网页检索意图识别。除“搜索网页/联网查询”外，含 URL 的“查看/查一下”请求，以及含天气、今天、最新、实时等实时信息关键词的查询，均授权 `web_search`；本地文件/工作区检索保持优先，不改变普通闲聊的默认离线策略。验收：截图中的天气与 URL 两类请求进入网页检索，文件查询仍只进入本地文件工具。

修复结果 P23-2b：已完成。策略现支持 URL、天气/日期、最新、实时、新闻、价格、汇率、比分和航班等明确网络信息意图；运行时工具任务判定直接复用工具策略，避免授权与模型路由不一致。天气请求、URL 请求、文件检索优先级与确定性网页检索回归共 4 项通过；服务重启后即可使用。

实施批次 P23-3：将网页工具路由升级为可解释的意图判定机制。先排除本地文件/工作区范围，再根据 URL、明确网络请求、时效性事实与外部来源/资料需求计算网页检索置信度；达到阈值才授权网页检索，并将路由原因写入执行上下文和运行审计。普通知识问答保持离线，后续工具继续复用同一策略层。验收：多类正负样本稳定路由、每次工具授权可说明原因、无需为每个新场景散落修改运行代码。

实施结果 P23-3：已完成。工具策略新增 `ToolDecision`，集中返回授权工具、置信度和原因；本地文件范围优先，URL/明确网络请求/时效性公开信息/外部资料来源按评分授权网页检索，普通解释性问答不联网。执行上下文与运行事件记录工具判断，运行详情可查看原因。网页、外部资料、本地文件、普通问答与确定性调用共 5 项回归通过。

实施批次 P26-5：为每个项目或对话文件夹增加“新建对话”入口。创建后对话默认写入当前文件夹，并自动切换至该对话；顶部新建对话仍保持未归类默认。验收：项目/对话文件夹均可创建归属对话，文件夹排序、重命名、删除和已有对话功能不受影响。

实施结果 P26-5：已完成。每个文件夹管理区新增“+”入口，点击后通过现有对话 API 创建“新对话”并写入对应 `folder_id`；文件夹自动展开、页面切换至聊天并聚焦输入框。项目与对话文件夹均适用，顶部新建对话未改动。已通过前端语法、项目/对话文件夹归属、删除保留与 diff 格式回归。

规划与实施批次 P27：搭建标准 MCP 工具接入能力，并以 Tavily Remote MCP 作为首个远程服务。范围包括：基于官方 Streamable HTTP 协议的初始化、工具发现与调用；不可变服务注册表；仅环境变量引用的凭据配置；工具命名空间、最小权限意图策略、超时/响应上限、运行审计和故障隔离。不得将带密钥的服务 URL、原始凭据或远程工具参数写入 Git、数据库或前端。先用本地 MCP 兼容服务回归，再做一次受控 Tavily 连通性验证。验收：未配置服务零网络请求；显式授权请求可调用发现到的只读工具；上游错误受控返回；来源与 Run 可追溯；现有 REST 网页搜索可作为兼容回退，直至 MCP 路径稳定。

实施结果 P27：已完成首个 MCP 工具底座与 Tavily Remote MCP 接入。客户端实现标准 Streamable HTTP JSON-RPC 生命周期（初始化、`notifications/initialized`、`tools/list` 分页、`tools/call`）、JSON/SSE 响应、协议版本、会话 ID、超时、响应上限和 `certifi` TLS 校验；服务配置仅保留环境变量引用与工具白名单。Tavily 仅允许 `tavily_search`，MCP 失败时回退既有 REST 搜索；其余远程工具不暴露。Python 编译与 diff 检查通过，最小真实 MCP 初始化、发现和单条检索成功（5 条来源），未输出凭据或结果内容。

修复批次 P27-1：排查明确要求 MCP/联网查询时模型仍返回“未授权工具”的问题。读取对应 Run 的执行上下文与事件，确保明确 MCP 或网页检索请求必经平台强制工具路径；模型只能根据实际工具事件回答，不能自行否认或虚构能力。验收：Run 记录 `web_search` 工具调用/结果，MCP 成功或受控回退可追溯，回答不再出现与运行事实矛盾的工具状态。

修复结果 P27-1：已完成。已确认截图对应 Run 实际授权并执行了 `web_search`，通过 Tavily MCP 获得 5 条来源；故障是预取后移除工具定义时，系统提示错误宣称“未授权工具”，与运行事件矛盾。现在预取成功会写入 MCP 后端标识、工具结果摘要和来源，并向模型明确“已通过 Tavily MCP 实际检索”，禁止否认工具状态；MCP 失败时才允许 REST 回退。MCP 状态一致性回归 2 项通过。

修复批次 P26-6：修复从文件夹新建对话后标题固定为“新对话”的问题。文件夹快捷创建时要求输入对话名称，文件夹归属保持不变；顶部新建对话仍沿用首条用户消息自动标题。验收：文件夹内新建对话创建后即显示用户输入的可读标题，不出现默认占位名。

修复结果 P26-6：已完成。文件夹“+”入口会先要求输入名称，取消不会创建，空名称会明确提示；创建 API 使用该名称与原文件夹 ID。顶部新建对话和首条消息自动标题逻辑未修改。已通过前端语法、diff 格式和本地静态资源检查。

修复批次 P26-7：调整文件夹“+”新建对话语义。点击时仅打开新建对话页面并暂存目标文件夹，不创建空对话；首条消息发送时才创建记录，以首条消息自动生成标题并归入目标文件夹。离开或刷新未发送的页面不得留下对话记录。验收：文件夹快捷入口不立即调用创建 API，首条消息后才出现归属正确的历史对话，顶部新建对话仍默认未归类。

修复结果 P26-7：已完成。前端新增临时文件夹归属状态，文件夹“+”只打开新对话界面；首条消息携带 `folder_id`，后端在创建新线程时校验该归属并写入。创建后的标题取首条消息摘要，未发送即离开不会产生记录，顶部新建对话清除临时归属。已通过延迟创建、文件夹归属、删除保留、Python 编译、前端语法和 diff 格式回归。

发布批次 R2：整理 P23-2 至 P27 的工具意图路由、Tavily REST/MCP、输入快捷键、项目/对话文件夹与左侧体验改动，更新 README 并推送 Git。验收：相关回归和格式检查通过；提交严格排除 `.env`、本地数据库和数据目录、依赖/发布目录、用户上传技能与无关删除。

发布结果 R2：已完成。完整回归 59 项通过，同时通过 Python 编译、前端脚本语法和 Git 差异格式检查；README 已补充项目/对话文件夹、首条消息延迟建档、工具意图路由、Tavily REST 与标准 Streamable HTTP MCP 的使用说明和边界。提交范围仅包含平台代码、测试和文档；`.env`、本地数据/依赖/发布目录、用户上传技能及既有无关技能删除均不纳入提交。

运维批次 O1：基于 `d340908` 重新启动本地 8765 服务，确保运行进程加载本轮 MCP、工具路由与前端改动。验收：旧监听进程已停止，新服务健康检查正常且页面可访问。

运维结果 O1：已完成。旧 8765 服务已停止，最新代码已重新启动；`/api/health` 返回数据库就绪、模型已配置与开发环境状态正常。

### P25：上下文续航与来源/产物体验

目的：在长对话中维持稳定判断能力，并让每轮对话的资料来源、工具结果和文件产物在前台可见、可追溯。

实施项：

- [x] 定义可配置上下文预算，按交接摘要、近期 12 条消息和新输入估算；超出预算前沿用既有早期摘要压缩。
- [x] 摘要与近期消息仍超限时，自动创建关联续聊线程，写入交接摘要和父线程 ID，并复制会话级技能选择；原对话保持完整历史与审计链。
- [x] 对话右侧面板聚合知识库命中、网页检索来源与文件产物；仅展示安全摘要、网页链接或下载入口，不展示本地绝对路径和敏感参数。
- [x] 续聊线程通过 SSE 返回新线程 ID，前端自动切换并刷新运行记录、来源和产物面板；空状态与 API 错误沿用现有明确提示。
- [x] 覆盖网页来源审计、上下文自动切换、交接摘要与版本回滚 API；原有来源去重、用户线程权限与产物删除回归继续通过。

完成标准：长对话不会因上下文失控而静默降质；用户能看到回答依据和实际产物，并可从任何展示项回溯到对应运行记录。

执行批次 P25-1：在既有“早期摘要 + 最近 12 条消息”压缩策略上，引入可配置上下文预算。预算仍超限时自动创建关联续聊线程，保存交接摘要和来源 Run，原线程保持可追溯。补充来源面板的网页来源显示与自动续聊 API 回归。

执行结果 P25-1：已完成。风险：自动续聊阈值默认 `MAX_CONTEXT_TOKENS=8000`，上线前应根据各实际模型的上下文窗口和成本目标调整；当前仍为单用户本地 SQLite，不包含跨设备的冲突协商。

### 后续推荐顺序

`P23 受控网页检索` → `P24 技能包治理` → `P25 上下文与来源体验`。每一阶段开始前，先在本文档补充该批次的目标、文件范围、验收标准和风险，再进行实现。

## 13. Agent 平台能力深化路线（P28-P36）

### 本轮执行约定与混合开发分工

目标：从“功能完整的单 Agent 平台”演进为“可评测、可记忆、可恢复、能稳定完成真实任务的个人工作 Agent”。本轮优先提升正确性与可验证性，不以增加模型、框架或页面数量作为完成标准。

开发方式：

- Codex 负责总体架构、计划维护、核心运行时与数据契约、主工作区集成、安全边界、代码审查、全量回归和最终验收。
- Claude Code 只在隔离 Git worktree 中处理文件范围明确、输入输出契约固定、可独立测试的子模块；本轮首个委派范围是“任务路由评测集与报告器”，不得修改 HTTP API、数据库 Schema、模型 Provider 或主运行时。
- Claude Code 的交付必须包含实际文件、独立测试和变更说明。Codex 审查通过后才集成；不符合接口、安全边界或测试要求的结果不进入主工作区。
- 每个阶段均遵循“先建立基线或测试 → 实现 → 定向回归 → 全量回归 → 记录结果”。未经用户明确要求，不提交或推送 Git。

执行约定变更（P32 起生效）：用户决定停止后续 Claude Code 协作。P32-P36 及其他未完成规划全部由 Codex 独立完成；不再创建 Claude 隔离 worktree、不向 Claude 服务发送任务上下文，也不再计算 Claude 产物采纳率。既有混合开发记录不删除，以保留真实决策与执行历史。

### P28：评测驱动的任务与模型路由

目的：用可解释、可回归的决策替代分散的关键词判断，稳定控制模型、工具、知识库、质量检查、延迟和成本。

实施项：

- [x] 将任务档位、工具需求、知识需求和质量检查决策收敛到独立 `TaskRouter`，保持现有 API 与默认行为兼容。
- [x] 路由输出采用结构化契约：`task_tier`、`needs_tools`、`needs_knowledge`、`quality_check`、`confidence`、`reasons` 和用户覆盖来源。
- [x] 保留确定性安全规则：工具权限仍由 `ToolPolicy` 决定，分类器或模型不得扩大权限。
- [x] 建立正负样本评测集，覆盖短但复杂、长但简单、工具意图、本地资料意图、普通离线问答和用户手动覆盖。
- [x] 输出路由准确率、逐字段差异和失败样例；模型路由变更必须先通过基线比较。

文件范围：`server/task_router.py`、`server/app.py`、`server/evals/`、对应测试；不修改前端 API 合同。

验收标准：路由结果可解释；安全权限不回退；现有回归通过；新增路由样例全部通过；同一输入产生确定性结果。

已知风险：规则仍不能等价于真实语义理解。本阶段先建立可评测的确定性基线，后续只有在评测证明收益时才增加低成本模型分类，并保留规则兜底。

### P29：混合 RAG 与证据质量

目的：提高本地资料召回率、片段相关性和引用可信度，使知识库能够支撑真实项目问答。

实施项：

- [ ] 为知识片段补充标题、章节、页码/位置、内容哈希和规范化文本等元数据。
- [ ] 建立关键词检索与向量检索并行召回、结果去重和分数归一化；向量能力未配置时安全退回关键词检索。
- [ ] 增加相邻片段扩展、注入预算和可选 rerank；同一事实避免重复注入。
- [ ] 增加“证据不足”判断，区分资料事实、模型推断和建议。
- [ ] 建立检索命中率、引用准确率、删除后不可召回和用户隔离评测。

验收标准：基准集 Recall@K 和引用正确率优于当前关键词基线；无向量服务时功能不降级；资料删除和用户隔离保持严格。

风险与授权：选择外部 Embedding 服务会产生网络、密钥和成本影响，实施前必须单独确认；首版优先支持本地或可选 Provider，不强制新增依赖。

### P30：结构化 Context 与交接

目的：避免多轮摘要反复压缩造成事实漂移，稳定保留目标、约束、决策和待办。

实施项：

- [x] 定义结构化上下文：目标、已确认决策、约束、实体、开放问题、待办和来源。
- [x] 每条结构化结论保存来源消息 ID、更新时间和状态；用户纠正后可覆盖或失效旧结论。
- [x] 上下文压缩和自动续聊使用同一结构化交接契约。
- [x] 注入模型前按任务选择最小相关字段，避免把完整项目状态塞入每轮 Prompt。
- [x] 对长对话、用户纠正、跨线程续聊和摘要幂等性建立回归。

验收标准：关键约束在长对话和自动续聊后不丢失；用户纠正不会与旧事实同时生效；上下文体积可量化。

### P31：显式长期 Memory

目的：让平台在用户可控的前提下记住偏好、项目事实和已确认决策。

实施项：

- [x] 定义 `preference`、`project_fact`、`decision` 三类记忆，以及作用域、来源、置信度、有效期和状态。
- [x] 采用“候选提取 → 敏感信息检查 → 用户确认 → 使用审计”的写入流程。
- [x] 提供查看、搜索、修改、停用和删除入口；删除后不再进入上下文。
- [x] 每次运行记录实际使用的记忆 ID，不允许模型声称使用未注入的记忆。
- [x] 建立错误记忆纠正、作用域隔离、过期和删除回归。

验收标准：长期记忆完全可见、可控、可删除、可追溯；默认不保存密钥、认证信息或大段资料正文。

### P32：Tool 风险分级与真实工作闭环

目的：在不牺牲安全性的前提下，让 Agent 从“回答问题”扩展到“完成工作”。

实施项：

- [x] 固化 `read_only`、`local_write`、`external_write`、`destructive`、`privileged` 风险等级和确认策略。
- [x] 将现有文件审批升级为结构化、可恢复的操作审批，展示白名单化参数、风险等级、影响摘要与幂等键；非只读工具默认不向模型暴露。
- [x] 增加限定目录的文件内容读取、网页页面读取等高价值只读工具。
- [x] 写工具要求幂等键、冲突检测、结果审计和可用时的回滚信息。
- [x] 工具调用建立成功率、误触发率、确认拒绝率和耗时指标。

验收标准：模型无法扩大权限；任一写操作都经过对应风险策略；重复恢复不会造成重复副作用。

### P33：Skill 能力包与治理

目的：将 Skill 从 Prompt 片段演进为带输入、工具、资源、输出和验收规则的可复用能力包。

实施项：

- [x] 扩展 Skill Contract：触发条件、输入 Schema、允许工具、资源、步骤、输出 Schema、验收规则和 Eval 用例。
- [x] 运行时只加载任务需要的资源；资源访问和工具授权分别校验。
- [x] 包内脚本只有进入受控执行器、通过超时/目录/依赖限制并获得相应确认后才可执行；默认继续禁止。
- [x] 为每个内置 Skill 建立最小评测，展示版本间质量差异。
- [x] 以 `research-brief` 完成首个端到端能力包试点。

验收标准：Skill 的输入输出和权限可验证；历史运行能还原确切版本；Skill 更新不会静默扩大工具范围。

### P34：持久化工作流与 LangGraph 评估门槛

目的：支持长任务、多次审批和重启恢复，同时避免过早引入复杂框架。

#### P34-0：执行模式显式化与路由可解释性（已完成）

目的：将当前“规则先路由、部分工具由模型决定是否调用”的隐式混合行为，转为用户可选择、每轮可复核的执行策略，降低知识库、联网工具和文件工具看似随机触发的体验。

实施项：

- [x] 为本地知识库、网络工具和文件读取分别提供 `off / auto / required` 三态覆盖；默认保持 `auto`，用户选择 `off` 时不得检索或向模型暴露对应工具，选择 `required` 时未执行或未命中必须在回答中明确说明。
- [x] 增加“回答依据”模式：通用回答、仅本地资料、仅联网资料、混合资料；它只收窄现有路由与可用上下文，不能绕过工具风险、用户隔离或资料来源边界。
- [x] 将网页检索、网页正文读取和工作区文件读取的执行策略统一为可审计合同：明确请求或 `required` 模式采用确定性预执行；`auto` 模式仍可向模型提供最小工具集，但必须记录“已授权未调用”的原因。
- [x] 在发送前显示简洁的自动判断摘要；在 Run 详情固定展示本轮任务档位、模型、知识库策略/命中数、网络/文件工具策略与实际调用、记忆注入数、来源模式和路由理由；不展示私有 Prompt 或思维链。
- [x] 将用户手动覆盖、自动路由、工具授权、实际调用、知识库命中/未命中、记忆注入和最终来源写入冻结的 `Execution Context` 与事件链。
- [x] 建立正负样例评测：相同输入在 `off / auto / required` 下得到可预测的授权与执行结果；覆盖无资料、工具失败、模型未调用已授权工具、权限收窄和用户取消场景。

文件范围：`web/index.html`、`web/static/app.js`、`web/static/styles.css`、`server/app.py`、`server/task_router.py`、`server/tool_policy.py`、`server/agent_loop.py`、评测与 API/前端回归测试。默认不引入外部服务、数据库破坏性迁移或 LangGraph。

验收标准：用户能在发起前知道平台将如何使用资料和工具；同一输入在相同模式下路由和执行可复现；`required` 不会被模型静默跳过；所有实际来源、工具与记忆使用可从 Run 回溯；既有 `auto` 默认行为保持兼容。

风险与顺序：三态覆盖会增加界面复杂度，首版以紧凑控件和默认 `auto` 控制认知成本。P34-0 完成后，再实施下方 P34-1 的持久化工作流；只有真实工作流复杂度证明现有状态机不足时才评估 LangGraph。

执行结果 P34-0：已完成。输入框新增“执行方式”控件，可选择资料依据（通用/仅本地/仅联网/混合）以及知识库、网络、文件的 `off / auto / required`；资料依据模式会安全收窄对应三态。输入停顿后调用只读路由预览接口，显示真实任务档位、预估本地命中数、可用工具与必需能力错误，不创建对话或 Run。

后端将模式、命中数、记忆数、授权工具和路由理由冻结进 Execution Context 与事件链。`required` 的网页检索和工作区文件检索会在模型生成前受控执行；自动模式下模型未调用已授权工具时记录 `tool_not_called` 事件。运行完成后将实际预执行结果回写至同一上下文，Run 详情能显示策略、实际工具轨迹与必需能力问题，不暴露私有 Prompt 或思维链。

验收结果：模式校验、资料模式映射、工具收窄、无副作用路由预览、既有聊天/记忆/产物/取消流程与 Agent Loop 回归通过；前端语法、Python 编译和 Git 差异格式检查通过；完整服务端回归 112 项通过。P34-0 已关闭，下一步为 P34-1 持久化工作流与 LangGraph 评估。

#### P34-1：持久化工作流与 LangGraph 评估

实施项：

- [x] 将运行状态细化为 `planning`、`retrieving`、`executing_tool`、`awaiting_confirmation`、`reflecting` 和终态。
- [x] 每个步骤定义可序列化输入输出、幂等性、超时、重试和恢复规则。
- [x] 使用现有 SQLite 验证跨重启恢复与多审批点流程。
- [x] 当出现跨进程等待、并行分支、复杂循环或 Worker 需求时，用同一用例对比现有运行时与 LangGraph。
- [x] 只有迁移显著降低状态管理复杂度且不破坏现有审计契约时才引入 LangGraph。

验收标准：当前运行时先具备稳定节点契约；LangGraph 是基于真实瓶颈的替换选项，而非上线前置依赖。

执行结果 P34-1a（2026-07-16）：已完成首批持久化节点契约。`runs` 新增 `run_phase`、阶段更新时间和恢复策略；`run_steps` 新增序列化输入/输出、幂等键、超时、最大重试、已重试次数与恢复策略。运行会将计划、检索、生成、工具、反思、待确认和终态以受限转换写入事件链；API Run 详情与界面均显示当前执行阶段。既有单审批产物流程在等待、批准、取消、失败和完成时均同步推进阶段并保留步骤输出。

验证：新增阶段状态机及 API 合同测试；完整服务端回归 113 项通过。P34-1 保持进行中，下一批只聚焦“多审批点 + 跨重启恢复”验证，随后使用同一真实案例完成现有运行时与 LangGraph 的量化比较；未达到复杂度门槛前不引入 LangGraph。

执行结果 P34-1b/1c：已完成。SQLite 新增可排序的 `run_approval_requests` 队列，Run 详情同时返回当前审批和完整审批列表；旧 `run_confirmations` 仍可读取。批准一个节点后若仍有后续待审批请求，运行保持 `awaiting_confirmation`，不会提前执行；取消会收敛全部未决请求。启动恢复继续保留待确认运行，且历史确认记录可回显。

LangGraph 评估结论：当前真实流程为单进程、顺序节点、0 个并行分支、0 个循环、0 个后台 Worker，审批与重启恢复均由 SQLite 状态/事件契约覆盖。引入 LangGraph 会增加运行时依赖和迁移成本，却不能减少当前节点数量或审计字段；量化门槛未满足，结论为“不引入”。若出现至少一个跨进程等待、并行分支、循环重试编排或 Worker 用例，再用同一审计字段重评估。

### P35：质量、成本与运营指标（【】等待真实评价样本）

目的：让平台优化可以通过数据定位到模型、Prompt、Context、Memory、Skill、Tool 或 RAG 层。

实施项：

- 【】建立带人工标注或用户反馈的引用准确率基线；采集与指标已完成，等待真实用户评价样本后生成统计基线。
- 【X】指标只保存必要元数据，不保存密钥、完整 Prompt、模型私有推理或敏感正文。
- 【X】增加按任务档位、模型、Skill 和工具筛选的本地质量视图。
- 【X】发布或模型/Prompt 变更前生成基线对比报告，并设置最低回归门槛。

验收标准：任一质量下降可定位到具体版本和能力层；指标采集不改变用户数据安全边界。

执行结果 P35（自动化部分完成）：本地 `/api/metrics` 以用户隔离的 Run、事件和审批元数据聚合任务档位完成率、输入/输出 token 估算、时延、工具成功率、审批拒绝率、模型路由分布及知识命中/必需无命中率；不返回完整 Prompt、回答正文、密钥或推理内容。现有 Run 详情保留模型、Skill、工具、来源和质量检查快照，构成本地可追溯质量视图；完整服务端回归是模型或 Prompt 变更的最低发布门槛。引用准确率仍需带人工标注或用户反馈的真实样本，不能由“检索命中”替代。

### P36：多模态与应用生态（本地解析部分完成）

目的：在文本工作流稳定后扩展图片、复杂表格、网页操作和企业应用。

实施项：

- 【X】扩展模型能力注册表，明确 vision、structured output、tool calling 和上下文能力。
- 【X】为图片、PDF 页面和复杂表格建立受控解析与引用链；PDF 页码和 XLSX 工作表/单元格链路已完成，图片通过本地 OCR 启用。
- 【X】应用优先采用标准 MCP 或受控连接器，逐项声明读取/写入权限。
- 【X】外部应用写操作沿用 P32 的确认、幂等和审计契约。

触发条件：P28-P32 的质量与安全基线完成，并有明确用户场景；否则不进入关键路径。

执行补充 P36（2026-07-20）：本机已检测到 Tesseract，并启用 PNG、JPG/JPEG、WebP、TIFF 图片的受控本地 OCR。图片字节仅通过有界本地子进程解析，25 秒超时、单次 OCR 文本上限 20,000 字符；不调用外部视觉服务、不记录额外正文副本。OCR 结果带稳定“图片 OCR（本地）”来源标识，经现有知识分片、项目隔离、引用与删除路径处理。知识库上传界面已显示本地处理说明并允许这些格式；PDF 与 XLSX 原有本地来源链保持不变。

### 总体执行顺序与阶段门

1. 质量基础：`P28 路由评测` → `P29 混合 RAG` → `P30 结构化 Context`。
2. 个性化与执行：`P31 长期 Memory` → `P32 工具闭环` → `P33 Skill 能力包`。
3. 规模化：`P34 可恢复工作流` → `P35 质量运营` → 条件触发 `P36 多模态与生态`。

本轮立即执行范围：P28 第一批。Codex 完成路由契约、核心实现和主运行时集成；Claude Code 完成隔离的路由评测集与报告器；Codex 负责审查、集成、全量回归并记录结果。P29 及以后在 P28 验收通过后逐阶段启动，涉及外部服务、新依赖或写操作权限时重新说明影响并取得授权。

执行结果 P28-1（Codex 核心）：已新增独立、无框架依赖的 `TaskRouter`，统一输出任务档位、模型、工具/知识需求、质量检查、置信度、解释原因和用户覆盖来源；`ToolPolicy` 仍是工具授权的唯一权威，手动选择不支持工具的外部模型时会回退到工具兼容模型。路由摘要已进入冻结的 Execution Context，现有 `/api/chat` 与前端合同未改变。新增 6 项路由单元测试，与工具策略合计 9 项通过；包含 API、运行时、模型 Provider、工具、扩展、运维、模型注册、供应商配置和网页检索的完整 65 项回归通过，Python 编译与 Git 差异格式检查通过。首次 API 测试因受限沙箱禁止绑定回环端口而失败，获准在本机回环环境重跑后全部通过。

Claude Code 协作结果：用户明确授权必要任务描述和代码上下文发送给外部 Claude Code 服务后，在隔离 worktree `/private/tmp/agent-p28-claude`、分支 `codex/p28-claude-evals` 中完成评测集、报告器和测试原型。Claude Code 2.1.211 相比升级前的 2.1.81 存在非交互兼容差异：`acceptEdits` 和普通 JSON 输出的多工具任务会停在 `tool_use`；改用 `auto` 权限、`stream-json --verbose`、Sonnet 低推理强度和单文件任务后可稳定执行。全程未使用跳过权限模式，未提交代码，未修改主工作区。

执行结果 P28-2（Claude 原型 + Codex 审查集成）：Claude 原型提供 24 条中文路由样例、标准库报告器和测试，并通过初跑暴露 19 条路由期望差异与 3 个输入校验缺口。Codex 审查后修正本地知识语义、空模型/非法任务档位校验、长但简单转换和短但复杂推理规则，压缩测试范围并集成主工作区。最终路由评测 24/24 通过；新增评测器测试 5 项通过；包含原有平台能力的完整 70 项回归通过，Python 编译和 Git 差异格式检查通过。回归中出现既有 SQLite `ResourceWarning`，未导致失败，记录为后续连接生命周期治理事项。

### 执行批次 P29-1：零外部依赖的本地混合检索基线

目标：在不引入外部 Embedding 服务、新密钥或网络成本的前提下，将现有中文二元词计数升级为可解释、可评测的本地混合检索，为后续向量召回建立稳定基线。

范围与分工：

- Codex 负责检索契约、关键词/短语/字段加权、长度归一化、相邻片段扩展、去重、注入预算、知识库 API 集成、安全隔离和全量回归。
- Claude Code 在新的隔离 worktree 中只负责合成检索评测样例与标准库报告器原型；不得接触用户本地知识文件、数据库、`.env`、HTTP API、主运行时或前端。
- 本批次不调用外部 Embedding、rerank 或模型 API，不安装新依赖，不修改现有知识库上传格式。

实施项：

- [x] 新增独立 `KnowledgeRetriever`，以文档标题、连续短语、查询词覆盖率、词频和片段长度归一化组合评分。
- [x] 返回结构化检索证据：文档 ID、文件名、片段位置、分项得分、匹配词、摘录和相邻片段来源。
- [x] 对命中片段执行同文档相邻扩展、内容哈希去重和总注入字符预算，避免重复或无界上下文。
- [x] 保持按用户隔离、删除后不可召回、无命中时不伪造依据；现有 API 与前端引用合同保持兼容。
- [x] 建立至少 20 条合成检索样例，覆盖标题命中、短语命中、词法边界、噪声文档、相邻片段、无命中、预算和稳定排序。
- [x] 输出 Recall@K、首条命中率、无关结果率和失败样例；以原关键词算法作为基线对比。

文件范围：`server/knowledge_retrieval.py`、`server/app.py`、`server/evals/knowledge_retrieval.json`、评测脚本与对应测试。数据库 Schema 只有在元数据无法由现有行计算时才允许追加兼容列，本批次默认不迁移。

验收标准：合成基准 Recall@4 与首条命中率达到 100%，无命中样例不返回结果；用户隔离、删除、现有引用和 API 回归通过；检索结果在相同输入下确定性排序；完整平台回归无退化。

风险与阶段门：纯词法方案无法可靠解决真正的语义同义词。本批次只建立可解释基线；P29-2 是否增加本地或外部 Embedding，必须依据失败样例和指标决定。外部 Provider 仍需单独授权。

执行结果 P29-1：已完成。新增独立本地 `KnowledgeRetriever`，综合短语、标题、词频、覆盖率、IDF 与长度归一化进行确定性评分，并实现同文档相邻片段扩展、规范化内容哈希去重、单片段/总字符预算及可解释分项得分。知识库 API 保持原合同，数据库查询继续先按用户过滤；上传—引用—删除链路及跨用户隔离均通过回归。

评测结果：Codex 审查 Claude Code 提供的 8 条样本设计后，按实际检索契约扩展为 20 条合成评测。新检索器 Recall@4、首条命中率、无匹配准确率和相邻片段准确率均为 100%；原关键词基线 Recall@4 为 87.5%，首条命中率为 93.3%。完整平台 78 项测试通过，Python 编译和 Git 差异格式检查通过。

Claude Code 升级影响：2.1.211 能完成样本分析与 `Write` 工具参数生成，但长任务持续输出 `thinking_tokens`，且缩小任务后初始化信息显示 `auto` 被降为默认权限，隔离 worktree 写入被拒绝。未使用绕过权限模式，未接触用户数据，主工作区无异常写入；Codex 仅采用其合成设计，经人工审查后用受控补丁集成。后续混合开发继续采用“小任务、低推理、隔离 worktree”，并在调用后校验真实文件，不把模型文字声明视为交付完成。

P29-2 阶段门：当前纯词法基准已经达到本批次质量门，因此暂不引入外部 Embedding、rerank 服务、密钥、网络调用或新依赖。下一步先进入 P30 结构化 Context；只有真实失败样例证明同义表达召回成为主要瓶颈时，再提出 P29-2 Provider 选型与单独授权。

### 执行结果 P30-1：结构化 Context 与可追溯交接

已完成。新增本地确定性 `StructuredContextBuilder`，将目标、约束、决策、实体、开放问题和待办保存为版本化 JSON；每条记录包含稳定 ID、来源消息 ID、来源角色、更新时间和 `active/superseded` 状态。明确的“目标改为”“取消”“不再”会失效旧条目，助手建议不会自动升级为用户已确认决策。

SQLite 线程表以兼容列保存完整结构化状态，原 `context_summary` 和 `handoff_summary` 继续作为可读展示。每次运行只冻结经过字符预算和任务相关性选择的最小上下文；`/api/threads/{id}/context` 返回完整可审计状态。自动续聊复制同一 JSON 契约，新线程可以继承旧目标和约束，同时允许新消息纠正继承内容。

Claude Code 协作结果：在隔离 worktree、禁用全部工具的条件下，2.1.211 成功返回 10 条合成长对话设计，但首个输出等待约 70 秒、总耗时约 78 秒。Codex 审查后采纳显式“目标/约束/问题”前缀场景，拒绝其将待办归类为决策的错误预期，并扩展为 12 条本地评测。首轮 72 项断言出现 2 个规则过宽问题，收紧“选择/确定/采用”分类后，最终 65 项字段、状态和来源断言准确率达到 100%。

验收结果：来源追踪、目标纠正、取消、长对话保留、最小注入、助手建议隔离和跨线程继承均通过；完整平台回归 86 项通过，Python 编译和 Git 差异格式检查通过。P30 已达到阶段门，下一阶段进入 P31 显式长期 Memory；Context 仍属于线程状态，不会自动转为跨线程长期记忆。

### 执行结果 P31-1：显式长期 Memory

已完成。新增本地 `memories` 与 `memory_usage` 数据契约，支持 `preference`、`project_fact`、`decision` 三类记忆，以及全局/项目作用域、来源消息、确认置信度、启停状态和过期时间。普通聊天不会自动保存；只有显式“请记住/我的偏好/项目事实/决定记住”才生成候选，候选仍需用户明确确认后才能写入。

安全与运行边界：密码、API Key、访问令牌和私钥样式内容在写入前被拒绝；选择记忆时同时校验用户、项目、启用状态、有效期、任务相关性、条数和字符预算。实际注入的记忆 ID 冻结在 Execution Context，并写入 `memory_usage`；系统提示只允许模型使用本轮列出的已确认记忆。停用、过期、其他用户或其他项目的记忆不会进入运行上下文，删除会同时清理使用关联。

用户入口：新增“长期记忆”页面，可创建个人偏好、项目事实和已确认决策，选择所有对话或具体项目，保存前二次确认，并支持搜索、修改、启停、查看使用次数和删除。API 同时提供候选、创建、列表、修改和删除合同。

Claude Code 协作结果：在隔离 worktree、禁用全部工具的模式下约 18 秒返回 8 条安全边界设计。Codex 采纳普通聊天不写入、密钥拒绝、作用域、隔离、停用/过期、更新与删除场景；拒绝无证据的 0.91 自动置信度，以及删除后继续保留完整正文审计的建议。扩展后的 12 条本地策略评测准确率为 100%。

验收结果：Memory 单元、评测和真实 API 生命周期通过；完整平台回归 93 项通过，Python 编译、前端 JavaScript 语法和 Git 差异格式检查通过。P31 达到阶段门，下一阶段进入 P32 Tool 风险分级与真实工作闭环。

### 执行结果 P32-1：工具风险合同与结构化审批

执行方式已按用户要求调整为 Codex 单独开发，不再调用 Claude Code。新增确定性 `ToolRiskPolicy`，固化 `read_only`、`local_write`、`external_write`、`destructive`、`privileged` 五级风险：只有只读操作可在既有授权范围内自动执行；所有写入和破坏性操作必须确认；特权操作还必须由用户明确发起；未知风险或未声明幂等能力的写工具无法注册。

工具注册器增加双重防线：需要确认的工具即使出现在允许 ID 中，也不会在未确认时暴露给模型，直接执行同样会被拒绝。现有文件产物审批升级为结构化操作记录，包含 `operation_id`、风险等级、工具 ID、白名单参数、影响摘要和幂等键；同一运行重复创建产物会返回既有文件，不产生重复副作用，审批拒绝和重启等待状态保持原恢复合同。

新增 `read_workspace_file` 高价值只读工具：只接受工作区相对路径和受支持文本类型，设置 20,000 字符上限，并拒绝 `.env`、SQLite 数据库、用户数据、依赖目录、Git 内部目录及路径越界。网页正文读取尚未加入：P32-2 必须先实现 HTTPS、DNS/IP、重定向、响应类型和体积的 SSRF 防护，不能用无边界 URL 请求代替。

验收结果 P32-1：风险策略、写工具隐藏/确认、结构化审批、产物幂等、拒绝与重启恢复、受限文件读取均通过；完整平台回归 98 项通过，Python 编译和 Git 差异格式检查通过。P32 保持进行中，下一批由 Codex 独立完成网页读取安全边界、通用写操作恢复合同和工具质量指标。

### 执行结果 P32-2：安全页面读取、回滚说明与工具指标

本批次继续由 Codex 单独实施。新增 `read_web_page` 只读工具，只接受 HTTPS、无凭据 URL 和 443 端口；每次请求及重定向都重新校验主机解析结果，拒绝本机、内网、保留地址和非全局 IP，并限制重定向次数、响应类型、响应体积、超时和正文长度。HTML 仅提取可见正文，脚本与样式不会进入模型上下文。工具策略只在 URL 与明确的读取、打开或总结意图同时出现时授权页面读取。

写操作合同补充可选回滚说明；当前真实写操作“创建文件产物”会持久化风险等级、白名单参数、影响摘要、回滚方式与幂等键，重复恢复返回已有产物，避免重复副作用。工具调用事件记录耗时，本地指标接口按当前用户聚合调用数、成功/失败、成功率、平均耗时、确认次数和确认拒绝率；指标不保存私有思维链。

验收结果 P32-2：安全网页读取、风险注册、写工具确认、文件读取、工具路由与 API 指标定向测试全部通过；完整服务端回归 101 项通过。P32 已关闭，后续进入 P33 Skill 能力包与治理，仍由 Codex 独立完成。

### 执行结果 P33：Skill 能力包与治理

本阶段由 Codex 单独完成。新增向后兼容的 Skill Contract，统一触发条件、输入/输出 Schema、允许工具、资源、验收规则与 Eval 用例；旧 JSON 和标准 `SKILL.md` 包会获得安全默认值，非法正则、Schema 或越界资源路径在导入时被拒绝。运行快照冻结技能版本与完整合同，历史运行可还原实际规则。

资源加载采用任务触发与字符预算双重边界，只读取已声明的 `references/`、`assets/` 文本资源；`scripts/` 继续只存储、不执行、不注入 Prompt。标记为工具范围技能时，其工具声明只与平台策略授权取交集，不能新增权限；既有文件产物技能保持独立审批合同，不会误收窄平台只读工具。

新增 `research_brief` 端到端试点，声明网页检索/页面读取权限、研究方法资源、结构化输入输出、验收规则和 3 条评测样例。5 个内置技能均已有最小合同评测，共 10/10 触发样例通过；评测器支持读取旧报告并输出逐技能版本与准确率差值。完整服务端回归 107 项通过，Python 编译与 Git 差异格式检查通过。P33 已关闭，下一阶段进入 P34 持久化工作流与 LangGraph 评估门槛。

## 14. 暂停与交接记录（2026-07-16）

暂停原因：当前 Codex token 即将不足，用户将切换模型或等待 token 重置后继续。此状态是主动暂停，不是工程阻塞。

当前检查点：

- P28、P29、P30、P31、P32、P33 均已完成；后续仍由 Codex 负责，不再调用 Claude Code。
- 最终完整服务端回归：107 项通过；Python 编译与 `git diff --check` 通过。
- 本机服务通过 macOS 用户级服务 `com.agentplatform.local` 运行于 `127.0.0.1:8765`；健康接口显示数据库就绪、DeepSeek 已配置，首页与登录接口返回成功。
- 本次提交只包含 Agent 平台规划与实现文件。不得纳入用户现有的 `data/`、`hoops-v2-upgrade/`、`node_modules/`、`sites-frontend/`、`server/skills/skill_a419791997528539.json`，也不得纳入用户对 `server/skills/translation_assistant.json` 的删除。

恢复执行顺序：

1. 检查当前分支、工作区和 `com.agentplatform.local` 服务状态；不要覆盖用户未提交内容。
2. 重新运行完整服务端测试，确认检查点仍为绿色。
3. 继续 P34-1：先使用现有 SQLite 完成跨重启与多审批点验证，再以同一案例比较现有运行时与 LangGraph。
4. 在评测证明现有状态机不足前，不引入 LangGraph。
5. P34 验收后再进入 P35 运行评测与可观测性，未经用户授权不发布、不增加外部服务或新密钥。

下一阶段状态：`P34-1 in progress`。P34-1a 已完成，待完成多审批恢复验证与 LangGraph 量化评估；当前没有未完成的 P33 实现任务。

### P31 补充修复：适用作用域内长期记忆的持续注入（2026-07-16）

问题：项目级 `decision` 记忆此前只在与当前消息存在词面重叠时注入。用户询问“本轮是否使用长期记忆”时不含记忆内容关键词，导致实际 Run 的 `memories` 为空；模型据此回答“未调用”，与用户对长期记忆的预期不一致。

修复：在用户、启用状态、有效期和项目作用域均通过校验后，`preference` 与 `decision` 作为持久行为上下文默认注入；`project_fact` 仍保持相关性筛选，避免无关项目资料挤占 Prompt。直接询问记忆使用状态时，会装载该作用域全部适用记忆以支持完整审计。系统提示明确区分“本轮实际注入”与“未注入”，要求模型仅据此如实回答。

验收：已复核截图对应历史 Run 的根因；修复后用同一用户、项目和问题验证会选中二次元项目决定。Memory 策略、Prompt 合同与真实 API 生命周期定向测试通过；完整服务端回归 109 项通过。服务 `com.agentplatform.local` 已重启并通过健康检查。

### P37：Agent 自主决策与执行闭环

目的：将“关键词规则授权后由模型可选调用”的单次生成路径，升级为可审计的结构化决策、证据充分性判断和可恢复多步闭环。

#### P37-1：结构化意图规划与检索充分性（当前执行）

- [x] 新增受控意图规划契约，输出知识库需求、澄清需求、置信度和理由；规则只作为安全边界与故障回退。
- [x] 当自动路由置信度低、问题与空间资料高度相关或首轮本地检索为空时，执行安全的回退检索或明确澄清，不静默直接回答。
- [x] 基于可解释的术语覆盖和分数判断检索是否足够；不足时最多一次受限二次检索，冻结查询与结果摘要到 Execution Context 和事件链。
- [x] 建立正负评测，覆盖隐式资料意图、同义表达、检索不足、空知识库、手动 `off` 和用户明确不检索。

#### P37-2：空间工作上下文

- [x] 将当前空间的近期任务和已产物以预算受限的结构化摘要注入；不跨空间泄露资料或成员信息。
- [x] 将空间上下文冻结在 Execution Context，支持从 Run 反查。

#### P37-3：Skill 工作流化

- [x] 为高价值 Skill 声明步骤、所需证据、工具顺序、输出契约、验收规则和失败降级。
- [x] 工具或产物失败时保留失败事件和可执行错误；模型必须按 Skill 验收规则说明缺少的证据、工具结果或用户输入，不得无依据完成回答。

#### P37-4：真实评测与反馈门槛

- [x] 建立本地匿名化 Run 审计与用户正/负反馈入口，记录隐式检索、低置信度、检索重试、资料不足和工具未调用。
- [x] 为模型、Prompt、Skill 或检索策略变更设置完整服务端回归门槛；不以知识命中率替代引用准确率。

阶段门：在 P37-1 的离线评测和线上审计均证明决策改善前，不增加外部 Embedding、后台 Worker 或 LangGraph。出现并行分支、跨进程等待、循环编排等真实需求时，再进入 P38 框架评估。

执行结果 P37-1a（2026-07-17）：已新增独立 `IntentPlanner`，用于识别任务路由未覆盖的隐式本地证据意图，例如“当前空间里已有资料”“公司制度”“上述内容”。在用户保持知识库 `auto` 时，规划建议会触发受限本地检索；用户显式选择 `off` 仍具有最高优先级。规划输出冻结到 `Execution Context`、Run 事件和无副作用路由预览，便于追溯实际决策。新增意图规划单元测试，并与既有任务路由、执行模式和路由预览回归通过。P37-1 后续仍需实现检索充分性判断、受限二次查询和澄清策略。

执行结果 P37-1b（2026-07-17）：已完成检索充分性与一次受限回退。首轮结果依据匹配术语覆盖与分数判定；不足时仅以原问题的规范化关键术语重试一次，合并并去重两轮命中，不增加工具权限或外部调用。`retrieval_trace` 会保存首轮查询、命中数、充分性、重试查询和重试命中数，并写入路由预览、Execution Context、`knowledge_retrieval_assessed` 与 `knowledge_retrieval_retried` 事件。意图规划、检索评测、资料上传/引用/删除和用户隔离定向回归通过。

执行结果 P37-1c 至 P37-4（2026-07-17）：资料需求但无充分本地证据时，`knowledge_route=insufficient` 会进入系统提示与最终来源边界，要求明确说明资料不足和可执行的补充方向。当前空间的近期任务与产物以最小、用户隔离摘要冻结在 Execution Context；Skill 提示增加完成前验收和缺失证据披露约束。`/api/metrics` 新增隐式知识检索、低置信度、检索重试、资料不足统计；`POST /api/runs/{id}/feedback` 支持用户对指定 Run 保存正/负反馈及可选备注，指标只聚合计数，不暴露反馈正文。完整服务端回归 117 项通过，P37 已关闭；P38 只有在真实出现并行分支、循环编排、跨进程等待或 Worker 需求时才启动。

执行结果 P35/P15/P36/P37 补充（2026-07-17）：资料来源区现在展示文件名、片段位置和受限摘录，并保留进入知识库查看命中的入口。Run 详情在实际使用本地资料时提供“引用准确 / 引用有误”评价；反馈表以可空 `citation_correct` 保存该独立维度，`/api/metrics` 仅聚合已评估数、正确数和准确率，样本为零时返回 `null`，不会把知识命中率当作引用准确率。模型注册表/API 现显式返回 streaming、tool calling、vision、structured output 和 context window；未声明的能力保持 false/null，不能被路由器假设为可用。P37 新增正/负向单元测试，覆盖同义本地资料请求、空库、无需资料时不重试；手动 `off` 已由执行模式回归覆盖。完整回归通过后，仍需累积真实用户的引用评价样本，才能发布有统计意义的引用准确率基线。

执行结果 P15/P36 补充（2026-07-17）：运行抽屉新增“全部审计”，按用户隔离列出最近 200 条运行，可按状态、任务档位和是否使用资料筛选，再进入单条 Run 查看模型、阶段、来源、工具与错误。知识库 PDF 解析现在为每页添加稳定的页码标记；XLSX 解析在本机直接读取共享字符串与工作表单元格，写入“工作表名 + 行值”的可检索文本，不上传第三方服务。图片仍未启用：当前所有已注册模型都明确声明 `vision=false`，平台拒绝把图片内容伪装为已解析；需先配置并验证视觉模型或本地 OCR 后才关闭该项。P38：`【】条件未触发`，当前没有并行分支、循环编排、跨进程等待或后台 Worker 的真实工作流证据，继续使用现有可恢复运行时，不引入 LangGraph。

执行结果 P15-工具执行（2026-07-17）：应用页的本地只读工具现在按输入 Schema 生成参数表单；用户可直接执行，并在失败后修改参数再次执行。每次手动执行均以用户隔离的审计记录保存工具 ID、参数键名、状态、耗时和不含原始内容的结果摘要；浏览器只在当前页面显示完整结果。手动执行不扩大权限，写操作不出现在该入口，仍必须走既有确认与幂等契约。

执行结果 P15-执行过程视图（2026-07-17）：对话中的 Agent 回复现将“执行过程”与“最终回答”分区展示。过程只呈现平台实际事件（模型选择、技能路由、计划、知识检索、工具调用、质量检查、确认与失败），生成时显示预计剩余 30 秒的倒计时及累计用时，完成后固定显示总用时；支持逐条查看与展开/收起。该视图不是模型私有思维链，不展示、不保存或推断模型内部推理。

执行结果 P15-推理摘要（2026-07-17）：新增默认收起的“推理摘要”。摘要只由本轮可审计执行状态生成：任务档位和模型选择、资料需求与命中数、允许工具范围、质量检查策略；它随 SSE 事件展示并写入 Run 事件，未读取、持久化或展示模型原始私有思维链。执行过程继续承载实时步骤，最终回答保持独立。

执行结果 P34-0-执行方式优化（2026-07-17）：默认执行方式从“资料、知识库、网络、文件”四个并列开关收敛为“回答依据”和“工作区文件”。回答依据提供智能选择、仅知识库、仅网络、知识库+网络；工作区文件保留自动/关闭/必须。知识库和网络的独立自动/关闭/必须能力收纳至“高级控制”，原有请求字段和路由约束保持兼容，避免默认界面重复表达同一选择。

执行结果 P34-0-模型配置优化（2026-07-17）：模型选择与任务深度由并列下拉框收敛至“模型配置”入口；收起时显示当前模型与深度策略，展开后保留模型和自动/快速/标准/深度的独立选择。提交字段、模型路由、输出预算和深度质量检查均未改变。

执行结果 P15-项目空间协作（2026-07-17）：原空间下的自定义文件夹定义升级为“项目空间”。项目空间成员可查看其中所有成员产生的对话、产物与来源；空间主页可直接发起新对话，新对话自动归属项目空间并标记创建者。对话、产物和来源展示创建者/关联对话；只有对话创建者可继续写入、改名、移动或删除自己的对话，其他成员为只读。邀请已有账户会立即加入；邀请尚未登录的邮箱会保留待加入状态，并在该邮箱首次登录时自动加入。成员管理仅由所有者执行。

执行结果 P15-项目空间对话界面（2026-07-17）：项目空间主页的对话入口改为复用主对话 Composer 的视觉层级和发送交互，沿用当前模型配置与执行方式。成员可在“对话对象”中选择 Agent_Platform 或项目成员；演示成员明确标注为演示数据，不会伪造真实多 Agent 调用。来源列表增加受限高度与内部滚动，避免长来源集合撑出页面底部。

执行结果 P15-项目空间协作界面补充（2026-07-17）：项目空间底部 Composer 增加共享范围与编辑权限提示，并通过输入区、操作区和阴影层级与主对话保持一致；窄屏下会自然换行。右侧对话条目现在明确展示“创建者：成员名”和当前用户是否可编辑，避免把共享可见误解为可编辑。

执行结果 P15-项目空间 Composer 一致性（2026-07-17）：项目空间不再维护独立的简化对话表单，而是直接挂载新建任务使用的同一 Composer。技能选择、模型配置、执行方式、路由预览、快捷发送与发送状态因此完全复用；项目空间仅额外提供成员/Agent 对话对象选择，并在发送时将新任务归属当前项目空间。

执行结果 P15-项目空间 Composer 视觉对齐（2026-07-17）：以新建任务作为唯一标准，移除项目空间内会挤压布局、且尚未连接真实协作调用的“对话对象”下拉项。项目空间仍在提交时自动归属新任务；其 Composer 的输入高度、控件行、间距与发送交互现与新建任务一致。

### P39：项目空间知识库

- 【X】P39-1：为知识资料建立通用/项目专属作用域、项目归属和上传来源元数据；迁移历史资料为通用资料。
- 【X】P39-2：知识库上传支持选择通用知识库或项目专属资料，并展示项目与上传来源标签。
- 【X】P39-3：项目空间右侧新增知识库卡片、空间内检索、固定归属上传与所有者删除入口。
- 【X】P39-4：普通任务仅检索通用资料；项目空间任务仅检索所在项目的项目专属资料，成员权限由服务端校验。
- 【X】P39-5：删除项目空间时同步删除其资料记录、检索分片和本地原文件。
- 【X】P39-6：补齐跨项目隔离、资料删除权限、历史引用脱敏与端到端回归。

执行结果 P39-同步编辑补充（2026-07-17）：知识库与项目空间展示同一知识资料实体。任一入口的上传、范围/项目归属编辑或删除均调用同一 API；完成后同时刷新知识库列表与当前项目空间知识区。资料编辑支持更名、通用/项目专属切换及目标项目空间调整；项目空间删除仍同步移除资料、分片和原文件。

执行结果 P39-6（2026-07-20）：历史对话与项目空间来源在展示时按当前查看者权限重新校验知识资料。资料已删除、转移或对当前成员不可见时，仅显示“资料引用已隐藏”标记，不返回文件名、摘录或文档标识；可见资料仍保持原有去重展示。项目资料删除继续仅限项目所有者，成员删除请求被拒绝。跨项目访问、通用/项目检索范围、删除权限与历史引用脱敏的端到端回归通过。

### P40：安全与运行时加固

- 【X】增加 CSP、反嵌入、MIME、防泄漏和浏览器能力限制响应头；前端维持同源静态资源加载。
- 【X】对带 `Origin` 的 POST/PATCH/DELETE 执行同源校验；保留无 Origin 的本机 CLI、测试和 Bearer API 调用。
- 【X】内存限流增加锁、活动窗口清理和用户数上限，避免长期字典增长与多线程竞态。
- 【X】技能目录全局快照改为锁保护仓库，写入后原子替换目录快照。
- 【X】Provider 网络错误改为结构化异常，区分认证、限流、上游、证书和网络故障，同时限制上游错误正文长度。

### P41：后端领域拆分


目标：在不改变 URL、请求/响应 JSON、数据库 Schema 与 SSE 事件契约的前提下，逐步收敛 `app.py`。每一子项都必须完成目标领域的定向回归和完整服务端回归后才可关闭。

- 【X】P41-0：冻结契约与迁移地图。列出认证、知识库、项目空间、聊天/SSE 的路由、数据库表、调用方和测试；为每个领域记录迁移前的 API 契约快照。
- 【X】P41-1：抽离认证服务。迁移密码哈希、会话读取/创建/失效、当前用户查询和本机管理员初始化；Handler 只负责 HTTP 输入/输出。验收：登录、登出、会话失效、生产管理员配置回归通过。
- 【X】P41-2：抽离知识库服务。迁移上传、解析、分片、通用/项目作用域、检索、编辑、删除和文件清理；项目空间仅调用服务接口。验收：通用/项目隔离、成员访问、双向同步、级联删除回归通过。
  - 【X】P41-2a：建立 `knowledge_service.py`，迁移资料列表与项目空间资料查询；上传/删除逻辑保持在兼容层。
  - 【X】P41-2b：迁移通用/项目作用域权限判断与检索接口。
  - 【X】P41-2c：迁移上传、解析、分片、文件存储与创建记录。
  - 【X】P41-2d：迁移编辑、删除、项目删除级联清理与文件删除。
  - 【X】P41-2e：Handler 改为只解析 HTTP 参数并调用服务；补齐双向同步回归。
  - 【X】P41-2f：完成完整服务端回归、健康检查和 Plan 状态更新。
- 【X】P41-3：抽离项目空间服务。已迁移空间、成员、邀请、任务/产物/知识聚合和权限判断；Handler 仅保留 HTTP 输入输出。验收：成员可见、创建者可编辑、所有者可管理、跨项目隔离回归通过。
  - 【X】P41-3a：建立 `space_service.py`，迁移空间访问、成员身份与所有者权限查询。
  - 【X】P41-3b：迁移空间创建、重命名、排序与删除前权限验证。已由 `SpaceService` 负责创建、所有者校验和排序更新；回归覆盖重命名、排序、成员删除拦截及删除后任务保留。
  - 【X】P41-3c：迁移成员邀请、自动加入、移除与角色权限。`SpaceService` 统一处理已注册成员即时加入、待邀请成员首次登录自动加入、重复邀请、成员移除与所有者保护；回归覆盖完整成员生命周期与非所有者权限拦截。
  - 【X】P41-3d：迁移空间任务、产物、知识资料与来源聚合读取。`SpaceService.get_space_detail` 在一次空间成员校验后聚合任务、产物、资料、成员、邀请及知识/网页来源；空间详情响应契约保持不变。
  - 【X】P41-3e：Handler 收敛为 HTTP 适配层，补齐成员可见、创建者可编辑、所有者管理、跨项目隔离回归。空间列表、访问校验和详情读取均改由服务提供；非法跨空间任务现返回 400 而非中断连接。
  - 【X】P41-3f：完整服务端回归、健康检查与 Plan 状态更新。46 项服务端回归按三批全部通过；编译、变更检查与本地健康检查通过。
- 【X】P41-4：抽离聊天编排服务。已迁移聊天请求/线程权限、上下文冻结、Run 创建、SSE 生命周期、确认和取消状态机；Handler 仅保留 HTTP/SSE 适配。验收回归通过。
  - 【X】P41-4a：建立 `chat_service.py`，迁移聊天请求校验、线程归属与项目空间编辑权限判断；普通对话、非法执行方式与成员只读回归通过。
  - 【X】P41-4b：迁移上下文冻结、路由、资料/记忆/空间上下文装配与 Run/步骤/事件创建。
  - 【X】P41-4c：迁移 SSE 执行、运行时事件落库、完成/失败/取消收口；Handler 只写 SSE。
  - 【X】P41-4d：迁移确认恢复、取消和重试的 Run 状态机入口及文件产物续跑。
  - 【X】P41-4e：普通对话、确认、取消、重试、文件产物、项目空间对话、运行恢复回归通过；46 项服务端回归分三批全部通过。
- 【X】P41-5：收敛路由与导入。新增声明式 `http_routes.py` 作为各 HTTP 方法的首层路由校验；统一通过 `python -m server` 启动，删除 `app.py` 的直接脚本导入 fallback。验收：包导入、关键 API 回归、健康接口与静态首页通过。
- 【X】P41-6：完成架构回归与交接。新增架构边界文档和防回流回归；全量模块编译、关键 HTTP 回归、路由评测 24/24、变更检查与健康检查通过；已记录 `app.py` 剩余职责和后续禁止回流规则。

P41-0 契约快照（2026-07-17）：认证覆盖 `/api/login`、`/api/logout`、`/api/logout-all`、`/api/me`，依赖 `users`、`sessions`；知识库覆盖 `/api/knowledge`、`/api/knowledge/search`、`/api/folders/{id}/knowledge`，依赖 `knowledge_documents`、`knowledge_chunks`、`thread_folders`、`space_members`；项目空间覆盖 `/api/folders`、成员/邀请与空间聚合，依赖 `thread_folders`、`space_members`、`space_invitations`、`threads`、`runs`、`artifacts`；聊天覆盖 `/api/chat`、路由预览、线程/Runs/确认/取消，依赖 `threads`、`messages`、`runs`、`run_events`、`run_steps`。前端调用保持 `/api/*` JSON 与 SSE 事件名不变。回归基线：认证/会话、资料上传/隔离/删除、项目成员与资料共享、执行方式/路由预览、对话重试/恢复/产物测试。

### P42：前端模块化与样式分层


目标：保持页面结构、浏览器存储键、接口调用和交互行为不变；每次只迁移一个视图或基础模块，并在桌面与窄屏完成冒烟检查。

- 【X】P42-0：冻结前端契约。已记录 DOM ID、全局状态字段、入口脚本、关键事件和页面切换路径；新增 `scripts/check-frontend.sh` 与前端契约文档。
- 【X】P42-1：抽离基础模块。已迁移 `state`、DOM 查询、API 客户端和本地存储；`app.js` 保留兼容入口，旧浏览器存储键不变。新增可重复执行的核心模块测试，覆盖令牌恢复、工作区状态恢复、鉴权请求头、API 错误状态以及 401 退出所依赖的契约；前端语法、差异、首页静态加载和健康检查通过。
- 【X】P42-2：抽离聊天与 Composer 模块。提示词与技能选择迁移到 `web/static/chat/composer.js`；流式请求、鉴权和 SSE 读取/解析迁移到 `web/static/chat/stream.js`；模型配置、执行方式和路由预览迁移到 `web/static/chat/execution-mode.js`；消息 Markdown 渲染迁移到 `web/static/chat/markdown.js`；确认与重试迁移到 `web/static/chat/interactions.js`；推理摘要、执行过程展开收起和倒计时迁移到 `web/static/chat/run-trace.js`。验收：前端核心模块检查、语法/差异检查、首页加载、浏览器输入与配置交互、以及 46 项本地 API 回归（覆盖流式、确认、取消、重试、运行详情）通过。
- 【X】P42-3：抽离项目空间与知识库模块。知识库的项目筛选、资料卡片和检索结果迁移到 `web/static/knowledge/library.js`；项目空间的成员、项目资料、对话和产物视图迁移到 `web/static/space/workspace.js`，保留既有上传、编辑、删除、邀请与双向刷新回调。验收：前端核心模块检查、语法/差异检查、首页加载和浏览器冒烟通过；项目空间成员/资料/对话/产物面板正常显示，知识库“项目专属”筛选可见且生效。
- 【X】P42-4：抽离其余视图。长期记忆与产物的卡片渲染、下载/删除和编辑/启停回调迁移到 `web/static/views/resources.js`；技能卡片和应用工具表单迁移到 `web/static/views/capabilities.js`；设置保存与退出会话迁移到 `web/static/views/settings.js`；运行审计列表、详情、产物下载、引用反馈和取消运行迁移到 `web/static/views/audit.js`。入口保留临时适配壳，重复旧详情实现纳入 P42-6 删除。验收：前端核心模块、语法/差异、首页和健康检查通过；浏览器验证技能/应用、长期记忆、产物、设置页面导航及主容器加载通过。
- 【X】P42-5：CSS 分层。已将样式按 tokens/base、layout、components、chat、space、knowledge、responsive 拆分，入口仅按既有优先级加载各层，并在前端检查脚本中校验所有分层文件。验收：本地浏览器已核验新建任务、项目空间、知识库和 390px 窄屏四种布局；窄屏无横向溢出，控制台无错误，静态检查、健康接口和全部样式资源加载通过。
- 【X】P42-6：移除兼容壳。已删除 `app.js` 中不可达的旧运行详情实现（由审计模块替代）及 Markdown、技能摘要、执行方式等重复转发函数；入口保留页面协调和事件绑定，功能实现统一由领域模块提供。样式入口只保留分层导入。验收：所有前端模块语法检查、核心模块测试、差异检查、健康接口和静态资源检查通过；浏览器冒烟覆盖新建任务、历史对话/运行详情、项目空间、知识库、技能和应用、长期记忆及产物，控制台无错误。

### P43：决策质量评测


目标：以可复现数据决定是否调整 Intent Planner、检索策略或 Prompt；不以单次主观体验或关键词堆砌替代评估。

- 【X】P43-0：定义指标与匿名化边界。已定义检索遗漏率、过检索率、引用准确率、资料不足澄清率和任务成功率；匿名 Run 仅包含盐化 ID 与决策元数据，不包含 prompt、回答、资料正文、用户 ID、URL、令牌或密钥；本地评测报告建议保留 30 天，详见 `docs/decision-quality.md`。
- 【X】P43-1：建立离线评测集。新增版本化的 `server/evals/decision_quality.json`，含 12 条应检索/不应检索/应澄清/项目隔离、任务档位用例，并固定为默认基线。
- 【X】P43-2：实现评测运行器与报告。新增 `server.evaluate_decision_quality`，可重复评分 Planner 与路由，并按模型、任务档位和项目/通用资料分组输出；固定集结果为 12/12 通过。
- 【X】P43-3：建立反馈闭环。评测器可选读取本地数据库的 Run 状态和 `citation_correct` 聚合列，不查询消息正文；少于 20 条引用评价或 30 条任务时强制标记“样本不足，不得宣称策略提升”。当前本地为 123 个 Run、0 条引用评价，未达到提升结论门槛。
- 【X】P43-4：受控优化实验。完成两个顺序、单变量实验：先收紧“是什么/说明”的宽泛事实检索规则，消除普通解释和方案写作的过检索；再将“说明”纳入隐式内部资料提问识别，恢复公司制度类资料检索。每一步均以固定集比较，最终遗漏率/过检索率/澄清遗漏率均为 0，任务成功率 1.0；`compare_experiment` 对门槛回退自动给出 `rollback`。
- 【X】P43-5：发布决策与运行门槛。新增 `decision_policy.version` 与质量门槛快照，冻结到每个 Run 的 `execution_context`；完整 API 回归验证该审计字段，后续 Planner/检索/Prompt 调整须通过固定集与反馈样本门槛后才可发布。

### P44：引用反馈驱动的检索优化闭环

目标：让“引用准确 / 引用有误”从结果统计升级为可诊断、可实验、可发布和可回滚的检索优化闭环。第一版只生成诊断与候选策略，不做在线自动学习，不因单条反馈直接改变检索结果。

#### P44-0：冻结基线与口径

- 【X】保存当前检索策略版本、`RetrievalConfig`、固定评测结果和引用准确率基线；样本不足时明确显示“暂不能判断改善”。
- 【X】区分三个指标：是否触发检索、是否召回正确文档、回答是否正确使用引用；禁止用单一“引用准确率”替代召回率或回答正确率。
- 验收：基线报告可重复生成；相同数据重复运行结果一致；不包含资料正文、回答正文、密钥或可导出的用户身份信息。

#### P44-1：细化反馈数据

- 【X】保留现有 `run_feedback` 兼容接口，新增文档级引用反馈表，关联 `run_id`、`document_id`、首次命中片段、检索策略版本、正误和原因码。
- 【X】引用有误时支持选择原因：文档不相关、片段不相关、资料已过期、回答误用了资料、缺少应有资料；备注保持可选且限制长度。
- 【X】同一用户对同一 Run 的反馈可修改，采用覆盖更新并追加审计事件；资料删除后只保留稳定 ID 和聚合统计，不保留额外正文副本。
- 验收：单文档、多文档、重复提交、修改反馈、资料删除、跨用户和跨项目隔离测试全部通过；旧版反馈仍能读取和统计。

#### P44-2：改造反馈交互

- 【X】运行详情先显示本次引用文档；“引用有误”后展开文档选择与原因选择，避免把回答错误误判为检索错误。
- 【X】提交成功后展示已记录状态，并允许用户修改；不展示“已自动学习”等误导文案。
- 验收：桌面与窄屏可完成准确、有误、原因选择和修改；无引用的 Run 不出现引用评价入口；刷新后能恢复已提交状态。

#### P44-3：建立诊断面板

- 【X】新增用户隔离的检索质量视图，展示评价样本数、正确率、错误原因分布、被误召回文档、遗漏反馈和检索策略版本。
- 【X】每项诊断可下钻到 Run、文档和分数构成，但正文只在原有权限范围内本地查看；聚合接口不返回消息或资料正文。
- 【X】设置最小诊断门槛：总体建议至少 20 条已评价引用；单文档至少 3 条负反馈才标记为高风险，低于门槛只显示观察项。
- 验收：零样本、小样本和足量样本状态正确；跨用户/跨项目不可见；统计可由原始反馈重复计算。

#### P44-4：生成可控优化建议

- 【X】以错误原因和现有 `score_breakdown` 生成候选建议：首版仅支持“候选资料数量”这一单变量；其他评分变量保留为后续候选项。
- 【X】每条建议必须包含证据样本量、目标指标、可能副作用、唯一变更变量和回滚配置；禁止同时自动修改多个变量。
- 【X】第一版只允许管理员创建候选实验，不直接修改线上策略；普通反馈用户只能查看自己的反馈结果。
- 验收：相同反馈数据生成相同建议；样本不足不生成发布建议；每个候选策略可序列化、版本化和还原。

#### P44-5：离线实验与质量门

- 【X】检索评测器对固定评测集运行基线/候选策略，计算召回、Top-1、无结果和邻接片段准确率；本地反馈样本按策略版本独立监测，不导出正文。
- 【X】发布门槛：候选策略不得降低固定检索评测的召回、Top-1、无结果或邻接片段准确率；项目隔离与权限回归保持通过。
- 【X】一次实验只允许一个变更变量；未达到样本门槛或质量门时自动给出 `rollback`，不得发布。
- 验收：提供至少一组会通过和一组会回滚的固定实验用例；报告包含基线、候选、差异、样本量和决策原因。

#### P44-6：受控发布、监测与回滚

- 【X】通过管理员确认发布新的检索策略版本；版本冻结到每个 Run 的 `execution_context` 和审计事件，支持一键恢复上一稳定版本。
- 【X】发布后按策略版本持续比较引用反馈，达到 20 条新评价前只显示观察状态；出现质量门回退时提示管理员回滚，不自动扩大权限或访问范围。
- 【X】补齐数据库迁移、备份恢复、API、前端、检索器、权限、项目空间和完整服务端回归。
- 验收：发布前后 Run 可追溯到准确策略版本；回滚后新 Run 使用旧版本、历史 Run 保持原记录；完整回归、恢复演练和健康检查通过。

建议交付顺序：P44-0/P44-1 为数据基础，P44-2/P44-3 形成可用反馈产品，P44-4/P44-5 建立优化实验，P44-6 才允许发布候选策略。P44-3 完成前不得宣称反馈提升命中率，P44-5 通过前不得让反馈自动影响线上检索。

执行结果 P44-0 至 P44-3（2026-07-20）：每个新 Run 现冻结 `lexical-retrieval-v1` 的检索参数快照；`/api/metrics` 返回该快照、文档级引用评价数量、准确率、错误原因分布和“至少 20 条样本”的发布门槛。新增 `citation_feedback_items`，在保留原 Run 级反馈接口的同时，按用户、Run、命中文档和首次片段保存评价、原因、可选备注及策略版本。运行详情已改为逐文档评价，可标记准确或有误、选择原因、填写备注并覆盖更新先前反馈；该反馈只进入审计与后续诊断，不会实时调整线上检索。运行详情新增“检索质量”视图，按当前用户聚合检索触发、找到资料、已评价文档相关、回答引用准确和资料缺失反馈等不同口径；资料观察项可回看关联 Run、首次命中片段位置及短语/标题/词汇/覆盖评分构成。聚合接口不返回对话或资料正文，单文档少于 3 条负反馈只作观察，整体少于 20 条文档评价明确标记为样本不足。检索策略快照、兼容反馈、文档级反馈、修改覆盖、诊断下钻和前端模块检查的定向回归通过。

执行结果 P44-4 至 P44-6（2026-07-20）：管理员可在“检索质量”视图中查看符合样本门槛的单变量建议、创建候选策略、运行固定检索集对比、确认发布或回滚至上一稳定版本。首版候选仅调整结果数量；建议由“缺少应有资料”或“文档不相关”反馈触发，且至少需要 20 条文档评价与 3 条同类原因。候选策略版本、父版本、证据、评测报告、状态和发布/回滚事件均持久化；新 Run 会冻结实际激活的策略版本。固定集要求候选不降低召回、Top-1、无结果和邻接片段准确率，回退则自动标记 `blocked` 并拒绝发布。检索质量视图按策略版本展示评价样本和引用准确率；每个版本少于 20 条新评价只显示观察状态。定向回归覆盖候选生成、评测通过、评测回退、发布、回滚、策略快照、反馈隔离与前端检查。

执行补充（2026-07-20）：通用知识库现可被所有项目空间任务检索，项目专属资料只在所属项目空间可见；对话和上下文侧栏均按文档去重展示来源。回答与对应 Run 建立持久关联，对话历史可回放已保存的推理摘要与执行事件；模型供应商若返回推理字段，仅记录其可用状态与长度，不展示原始内部推理。每条回答提供独立的“有帮助 / 没帮助”反馈，且该评价与文档级引用评价可以并存，不会互相覆盖。技能页将导入入口归入“技能”标签；“应用与工具”单独呈现已接入应用和仅供管理员验证的手动工具，避免将工具参数误解为对话检索配置。相关 API、模型 Provider、前端模块及定向回归通过。

### P45：单 Agent 智能执行闭环（核心实现完成；本机已按用户授权进入 Active）

#### P45-C：批次 C 导航（P45-3 的实施批次）

说明：P45-C 是 P45-8 定义的第三个实施批次，不是独立编号；它落实本节的 P45-3“状态化单 Agent 执行循环”，范围先限定为工作区与网页等只读工具。当前已完成状态机、Shadow 审计、Active 生命周期校验、工具预算收敛、失败后一次重规划，以及服务重启后的安全终止与可重试恢复；不会在重启后静默重放工具调用。

目标：把当前“规则路由 → 一次生成 → 通用检查”的回答工作流，升级为“受控规划 → 证据收集 → 缺口判断 → 工具行动 → 结果观察 → 任务验收 → 必要修订”的单 Agent 执行闭环。在不扩大权限、不暴露模型私有思维链、不引入多 Agent 和不破坏现有 API 的前提下，提升复杂任务的理解、主动查证、工具使用和完成度。

#### P45-0：代码级问题基线与不可回退边界

当前根因（基于现有实现核对）：

1. `TaskRouter`、`IntentPlanner` 与 `ToolPolicy` 主要依赖关键词和长度规则。它们适合权限前置和低成本基线，但不能充分理解隐式目标、复合约束和证据需求。
2. `file=auto` 只有在规则先识别到明确的“文件/目录/读取/查找”语义时才会向模型暴露文件工具；模型无法对未被规则授权的隐式文件需求主动补查。
3. `build_execution_plan` 生成的是通用展示步骤，不是模型根据目标、交付物、证据和风险生成的真实计划；计划也不会根据工具结果动态更新。
4. 知识检索充分性只依据首条结果的匹配词数量与分数阈值；一次词法重试后只要有结果就可能被视为充分，尚未判断资料是否覆盖用户要求的每个关键结论。
5. `SingleAgentLoop` 只有“模型 → 工具 → 模型”循环；模型第一次不发工具调用就直接结束，没有独立的证据缺口门、澄清状态、任务验收状态或基于失败结果的计划更新。
6. 深度任务一旦需要工具，当前模型路由倾向切回默认快速工具模型；复杂工具任务可能没有使用质量更高且支持工具的模型。
7. `reflect_answer` 只接收用户问题和初稿，检查目标覆盖、格式和矛盾；它看不到任务计划、验收标准、实际证据、引用映射和工具失败，因此无法可靠发现“证据不足但回答很流畅”。
8. 当前模型供应商推理字段只记录可用状态与长度，不参与计划、证据或验收决策；单纯展示或保存原始思维链既不能保证质量，也不符合安全边界。

不可回退边界：

- 确定性策略继续拥有身份、项目隔离、资料范围、工具权限、风险等级、确认、超时、最大调用次数和预算的最终决定权；模型只能在授权集合内提出动作。
- 通用知识库对所有项目可见，项目专属资料只对所属空间成员可见；任何模型规划不得扩大该范围。
- 不保存或展示模型原始私有思维链；只保存结构化计划、证据需求、工具理由、观察结果、验收结论和面向用户的推理摘要。
- 保持现有 `/api/chat`、SSE 基础事件、Thread、Run、确认、取消、重试和历史回放兼容；V2 任一步失败可回退现有 `SingleAgentLoop`。
- 第一阶段不引入 LangGraph、多 Agent、外部向量服务或新的写操作工具；先证明单 Agent 闭环能提升固定指标。

验收：新增基线报告明确记录现有路由命中率、知识检索充分性、工具调用率/有效率、无依据结论率、任务验收通过率、平均模型调用数、平均工具调用数、时延和 token 估算；没有足够真实反馈时必须标注“样本不足”。

#### P45-1：结构化 Task Frame 与模型辅助规划（Shadow Mode，批次 A 已完成）

新增版本化 `TaskFrame` 契约，至少包含：

```json
{
  "version": 1,
  "goal": "用户最终要完成的目标",
  "deliverables": [{"id": "d1", "description": "交付物", "required": true}],
  "constraints": [{"id": "c1", "description": "约束", "source": "user|context|policy"}],
  "evidence_requirements": [{"id": "e1", "description": "所需证据", "preferred_sources": ["knowledge", "workspace", "web"]}],
  "proposed_actions": [{"type": "retrieve|tool|clarify|draft", "reason": "面向审计的理由"}],
  "acceptance_criteria": [{"id": "a1", "description": "可验证完成标准"}],
  "ambiguities": [],
  "confidence": "high|medium|low"
}
```

实施项：

- 【x】P45-1a：新增 `server/task_planning.py`，提供严格 JSON Schema、字段校验、长度限制、稳定排序、去敏和解析失败回退；模型输出不能包含权限决定或原始思维链。
- 【x】P45-1b：只对 `standard/deep`、复合目标或低置信规则路由调用规划模型；`quick` 默认继续走现有路径，避免所有对话都增加成本。
- 【x】P45-1c：规划模型接收当前用户消息、最小结构化对话上下文、用户明确配置和可用能力摘要；不直接接收未授权资料正文或全部工具。
- 【x】P45-1d：以 `AGENT_INTELLIGENCE_V2=false` 和 `AGENT_PLANNER_MODE=off|shadow|active` 控制上线。`shadow` 只生成并保存 Task Frame，不改变现有检索、工具或回答。
- 【x】P45-1e：把 `planner_version`、耗时、token 估算、解析状态、回退原因和 Task Frame 摘要写入 Run 事件；不得记录完整系统 Prompt。

文件范围：新增 `server/task_planning.py`、`server/test_task_planning.py`、`server/evals/task_planning.json`；小范围修改 `server/app.py`、`server/chat_service.py`、模型 Provider 与 Run 事件展示。不得在本批次改写 `SingleAgentLoop`。

验收：已新增 30 条固定标签和离线 TaskFrame 评测入口；真实 Shadow 观察结果尚未积累，90% 质量门暂不宣称通过。单元测试覆盖权限类型拒绝、JSON 解析回退与关闭开关；接口回归证明默认关闭时 V1 行为保持不变。

执行结果（2026-07-20）：新增 P45 V1 元数据基线报告入口 `python3 -m server.evaluate_agent_baseline --database agent_platform.db`，小于 30 个 Run 会明确标为样本不足，且不导出对话或资料正文。TaskFrame 以默认关闭的 `AGENT_INTELLIGENCE_V2=false` 控制；启用 `AGENT_PLANNER_MODE=shadow` 后，仅对 standard/deep 或低置信任务调用规划模型，失败安全回退到确定性 TaskFrame。规划结果只作为 Run 审计事件和面向用户的安全摘要保存，不影响现有模型提示词、资料检索、工具授权或最终回答。`active` 在本批次按 shadow 处理，避免提前改变行为。

阶段门：P45-1 只验证规划质量，不得直接改变线上行为。固定集或权限测试未通过，不进入 P45-2。

#### P45-2：Evidence Ledger 与证据充分性门

执行进度（2026-07-20，批次 B 完成）：已新增知识库专用 Evidence Ledger。它仅保存需求覆盖状态和已授权来源的稳定片段标识，不复制资料正文；明确将“检索有结果”与“需求已覆盖”区分开。默认 `AGENT_EVIDENCE_MODE=off`；`shadow` 只记录评估，`active` 才会对未覆盖需求追加最多两次确定性、受现有项目/用户隔离约束的改写查询。标准/深度任务出现确定性缺口时，可使用模型在来源标识、文件名和命中词的最小视图上判断“继续检索 / 澄清 / 带限制回答”；模型不得将确定性缺口升级为“充分”，解析失败即回退。已新增 40 条固定样例及 `python3 -m server.evaluate_evidence_sufficiency` 评测入口，覆盖充分、继续检索、澄清和带限制回答；固定集全量通过。真实 Shadow 样本与跨项目隔离灰度仍需后续积累，不以固定集宣称线上提升。

补充进度（2026-07-20）：Evidence Ledger 现可追加网页检索与工作区检索的已授权来源元数据（稳定标识、支持的需求、相关性与时效），不保存正文；模型 next_action 已有严格契约校验，不能请求未授权工具。后续仍需让模型正式输出该契约并由编排器驱动，而不是仅复用既有工具调用协议。

新增版本化证据账本，明确区分“检索有结果”和“足以完成任务”：

```json
{
  "requirements": [{"id": "e1", "status": "covered|partial|missing|unavailable"}],
  "items": [{
    "id": "ev1",
    "source_type": "knowledge|workspace|web|user|memory|tool",
    "source_id": "稳定来源 ID",
    "supports": ["e1"],
    "relevance": "high|medium|low",
    "freshness": "current|unknown|stale",
    "permission_checked": true
  }],
  "decision": "sufficient|retrieve_more|clarify|answer_with_limits",
  "missing_requirement_ids": []
}
```

实施项：

- 【X】P45-2a：新增 `server/evidence_service.py`，把用户明确事实、知识库命中、工作区结果、网页来源、长期记忆和工具观察转换为统一证据元数据；正文继续保留在现有权限范围，不复制到聚合表。
- 【】P45-2b：先用确定性检查映射 Task Frame 的证据需求与实际来源，再由模型对 `standard/deep` 的 `partial/missing` 项做结构化充分性判断；模型不能把“有一个搜索结果”直接判为完整覆盖。
- 【】P45-2c：知识库不足时允许规划器生成最多 2 个受限改写查询；每次查询都走现有用户/项目隔离与 P44 检索策略，不得扩大资料范围。
- 【】P45-2d：当证据缺口可通过授权工具补足时输出 `retrieve_more`；需要用户提供关键目标、数据或范围时输出 `clarify`；工具不可用时输出 `answer_with_limits` 并要求最终回答明确边界。
- 【】P45-2e：修正当前词法重试的充分性语义：重试“有结果”不等于“充分”，最终状态必须来自需求覆盖判断；保留旧字段兼容并新增 `evidence_state`。

文件范围：新增 `server/evidence_service.py`、`server/test_evidence_service.py`、`server/evals/evidence_sufficiency.json`；修改 `server/knowledge_retrieval.py`、`server/knowledge_service.py`、`server/chat_service.py`、`server/app.py`。

验收：至少 40 条固定证据样例；“应继续检索”的召回率不低于 90%，“已有充分证据”的误判率不高于 10%，跨用户/跨项目泄漏为 0；缺少当前数据、配置内容或明确资料时不会生成确定事实结论；关闭 V2 后现有检索行为不变。

阶段门：先在知识库场景启用，不同时改工作区和网络。知识库证据门通过固定集后，才扩展其他来源。

执行补充 P45-2a（2026-07-20）：账本现统一记录用户当前输入、已选择的长期记忆、知识命中、工作区/网页结果及通用工具完成观察的稳定标识；只保存来源类型、标识、相关性和需求映射，不复制用户、记忆、文件或工具正文。用户和记忆来源不能将缺失的知识需求直接提升为“已覆盖”。

#### P45-3：状态化单 Agent 执行循环

执行进度（2026-07-20，批次 C 完成）：已新增独立的 `AgentOrchestrator`，以显式状态、合法转换表和 quick/standard/deep 调用预算约束单 Agent 流程；覆盖证据收集、评估、工具行动、观察、重规划、起草、验证、修订和终态。`AGENT_ORCHESTRATOR_MODE=shadow` 会将预演状态转换写入 Run 事件，不改变 V1 模型、工具、确认或最终回答；`active` 已对现有工作区/网页等只读工具循环接管生命周期校验，并按实际工具调用计数限制 V2 循环上限、持久化本轮预算。TaskFrame 存在时，Run 计划会按目标、证据需求和交付物生成动态步骤；工具实际开始会推进对应步骤，未调用的授权工具明确记录为“已跳过”。规划模型的 `next_action` 现有严格结构化契约：已授权的 `use_tool` 交给与 V1 相同的注册表、参数 Schema、白名单和预算路径实际执行；`draft_answer` 会禁用额外工具并只基于冻结证据起草；`clarify_user` 与 `complete_with_limits` 会分别进入澄清或带限制终态，不能继续生成貌似完整的回答。相同工具与相同参数在没有新证据时会被拒绝，模型必须修正参数或选择替代动作；工具失败仍最多允许一次受控重规划。规划调用也计入模型预算，并为最终回答预留调用额度。单次模型返回多个工具调用时，超出预算的调用会被明确拒绝并回传给模型，不会执行。服务重启会安全终止未完成 Run 并标为可重试，不会静默重放工具调用。工具授权、参数校验、确认和实际执行仍完全复用 V1 `SingleAgentLoop`；写操作不在本批次范围内。

新增 V2 编排器，但保留现有 `SingleAgentLoop` 作为稳定回退。状态固定为：

```text
PLAN
→ COLLECT_EVIDENCE
→ ASSESS_EVIDENCE
→ ACT（可选工具）
→ OBSERVE
→ REPLAN / ASSESS_EVIDENCE
→ DRAFT
→ VERIFY
→ REVISE（最多一次）
→ COMPLETE / CLARIFY / COMPLETE_WITH_LIMITS
```

实施项：

- 【】P45-3a：新增 `server/agent_orchestrator.py`，状态转移使用显式枚举和纯函数校验；每次转移写入 Run 事件，非法转移直接失败并回退，不以自由文本控制状态。
- 【】P45-3b：模型只输出结构化 `next_action`：`use_tool`、`retrieve_knowledge`、`clarify_user`、`draft_answer`、`complete_with_limits`；平台重新校验工具 ID、参数 Schema、权限、风险、确认、调用预算和重复调用。
- 【X】P45-3c：工具结果进入 Evidence Ledger 后必须重新评估缺口；工具失败时允许一次参数修正或替代动作，不能在没有新证据时重复相同调用。
- 【】P45-3d：建立分档预算：`quick` 默认 1 次生成、0～1 次工具；`standard` 最多 4 次模型调用、3 次工具；`deep` 最多 7 次模型调用、5 次工具、1 次重规划和1 次修订。用户手动指定档位只改变预算，不扩大权限。
- 【】P45-3e：达到预算上限时进入 `COMPLETE_WITH_LIMITS`，明确已完成内容、缺失证据和未执行动作；禁止静默生成看似完整的答案。
- 【】P45-3f：Run 步骤从通用占位升级为 Task Frame 动态步骤；已开始、完成、失败、跳过和等待用户的状态可恢复，仍沿用现有确认、取消、重试和幂等契约。

文件范围：新增 `server/agent_orchestrator.py`、`server/test_agent_orchestrator.py`；扩展 `server/agent_runtime.py`、`server/chat_service.py`、`server/app.py`，尽量不改变 HTTP Handler。旧 `server/agent_loop.py` 不删除。

验收：覆盖无需工具、单工具、多工具、工具失败、重复调用、证据不足、需要澄清、预算耗尽、用户取消、确认恢复和服务重启；所有路径都有终态，不能无限循环；权限与项目隔离回归 100% 通过；V2 异常可按 Run 回退 V1。

阶段门：先以 Shadow 记录 V2 决策，再对管理员账户启用；不得一次性全量切换。

执行补充 P45-3c（2026-07-20）：每个授权工具完成后，平台立即以来源类型和需求映射重新计算 Evidence Ledger 覆盖状态，并写入 `evidence_reassessed` 事件。工具观察本身不能冒充网页、工作区或知识来源；只有与需求允许来源匹配的新证据才可补足缺口。无新证据时，同工具同参数调用仍被拒绝；工具失败后仍受一次受控重规划限制。

#### P45-4：任务类型验收器与基于证据的修订

执行进度（2026-07-20，P45-D 完成）：已新增 `task_verifier.py`，使用 TaskFrame 交付物、Evidence Ledger 缺口、实际工具事件和候选回答执行确定性验收，输出通过状态、需求结果、无依据风险、缺失证据和建议动作。验收规则覆盖普通回答、知识回答、分析、方案与代码任务；工具失败但回答未说明未验证项会被拒绝。`AGENT_VERIFIER_MODE=shadow` 只记录验收事件，`active` 会在“只能带限制完成”的场景追加明确边界，并在验收建议修订时触发既有的一次受控反思/修订链路；修订后会再次执行确定性验收。新增 35 条固定验收样例及 `python3 -m server.evaluate_task_verification`，固定集通过；真实 Shadow 数据尚不足，不能据此宣称线上提升。

实施项：

- 【】P45-4a：新增 `server/task_verifier.py`，输入为 Task Frame、Evidence Ledger、工具结果摘要、引用映射和候选回答；不再只检查用户问题与初稿。
- 【X】P45-4b：按任务类型加载验收规则：普通问答检查直接性；知识回答检查引用支持；分析检查事实/推断分离；方案检查目标、行动、负责人、时间、风险和指标；代码任务检查相关文件、变更、测试与未验证项；文件任务检查实际产物存在。
- 【】P45-4c：输出结构化 `VerificationResult`：`passed`、`requirement_results`、`unsupported_claims`、`missing_evidence`、`action=complete|retrieve_more|revise|clarify`、`summary`。
- 【】P45-4d：只有出现新证据或明确的验收问题才允许修订；最多修订一次。修订后再次执行确定性验收，避免“检查失败但仍标记通过”。
- 【】P45-4e：模型检查不可用或 JSON 解析失败时，不再默认宣称通过；保留回答但标记 `verification_unavailable`，并在运行详情显示边界。

文件范围：新增 `server/task_verifier.py`、`server/test_task_verifier.py`、`server/evals/task_verification.json`；替换 `reflect_answer` 的 V2 调用路径，保留 V1 兼容函数。

验收：至少 35 条候选回答样例，覆盖完整、遗漏交付物、无依据结论、错误引用、工具失败后伪造结果和格式正确但任务未完成；严重问题召回率不低于 90%，完整回答误判率不高于 10%；修订不得引入新的无依据事实。

执行补充 P45-4b（2026-07-20）：代码任务除“变更/测试”表述外，还要求实际成功的变更与测试工具记录；缺少记录时只能明确标注未验证，不能宣称完成。文件任务在批准前或生成前不通过产物验收；文件实际创建并确认存在后，平台追加 `task_verified` 事件，记录产物阶段的验收结果。

#### P45-5：模型角色与成本质量路由

执行进度（2026-07-20，P45-D 完成）：Execution Context 已显式记录 planner/executor/verifier 三个角色及回退说明，当前默认复用同一已配置模型；三类角色的调用数、规划时延和 token 估算随 Run 保存。模型能力注册表补充 reasoning signal 与 JSON contract；`/api/metrics` 按当前用户聚合角色级调用数、token、时延和实际模型。深度任务即使需要工具，也优先选择高质量模型并再校验工具能力，避免无理由降级到快速模型。

实施项：

- 【】P45-5a：模型能力注册表增加 `supports_tools`、`supports_reasoning_signal`、`supports_json_contract`、上下文窗口和质量档位的真实配置；禁止仅凭模型名称推断能力。
- 【】P45-5b：区分 `planner_model`、`executor_model` 和 `verifier_model`。默认可复用同一模型，但复杂工具任务应优先使用“高质量且支持工具”的执行模型，而不是固定回退快速模型。
- 【】P45-5c：模型不可用时逐级回退，并在 Run 中记录原选择、回退模型和原因；不得把模型回退描述成用户主动选择。
- 【】P45-5d：按任务档位记录规划、执行、验证分别消耗的调用数、时延和 token 估算；设置单 Run 总预算与超限终止。
- 【】P45-5e：供应商返回的 reasoning 字段只作为“可用信号”和计量元数据；不将原始内容写入消息、Run 事件、日志或前端。

验收：相同任务在模型能力缺失、工具不兼容、上游失败和预算不足时均有确定回退；复杂工具任务不会无理由降到快速模型；成本与时延可以按角色聚合。

#### P45-6：对话交互与可审计解释

执行结果（2026-07-20，P45-E 完成）：对话历史 Run 回放新增默认折叠的“任务理解”和“证据与缺口”层，继续保留执行过程和最终回答；内容只读取已保存的 TaskFrame、Evidence Ledger、工具目的/结果摘要和验收事件，不展示 Prompt 或模型私有推理。证据决策为 `clarify` 时显示澄清卡，说明待补充项并引导用户在当前对话继续，后续 Run 在既有线程上下文中受控续跑。回答评价的“没帮助”新增未理解目标、资料不足、结论不准确、没有执行、过于啰嗦、格式不合适等可选原因，持久化到 Run 反馈；它与文档级引用评价保持独立，接口和历史回放兼容。前端核心检查与对话/知识库反馈回归通过。

实施项：

- 【】P45-6a：对话中保留面向用户的“任务理解”“证据与缺口”“执行过程”“最终回答”四层信息；默认折叠，不显示内部 Prompt 或原始思维链。
- 【】P45-6b：当 Agent 需要用户补充信息时，使用明确的澄清卡，说明缺少什么、为什么影响结果、用户可以如何补充；澄清后从同一 Run 或受控续跑点恢复。
- 【】P45-6c：显示工具调用的目的、结果摘要和是否解决证据缺口；不展示敏感参数或完整文件正文。
- 【】P45-6d：“自动判断”界面说明改为“平台先做权限与基础路由，Agent 再在授权范围内规划和补证据”；显示本轮实际采用的来源，而不是仅显示配置值。
- 【】P45-6e：回答评价增加可选原因：未理解目标、资料不足、结论不准确、没有执行、过于啰嗦、格式不合适；回答反馈与引用反馈继续独立保存。

文件范围：扩展 `web/static/chat/run-trace.js`、新增独立 Task Frame/Evidence 视图模块，修改 `web/static/app.js`、`web/index.html`、分层 CSS 与前端契约测试。

验收：桌面与窄屏均可查看计划、证据缺口、工具理由和验收结论；刷新后完整回放；无运行关联的旧消息正常降级；反馈不会覆盖引用评价；前端不出现私有推理文本。

#### P45-7：评测、灰度发布与回滚

执行结果（2026-07-20，P45-F 完成）：新增 `python3 -m server.evaluate_p45_rollout --database agent_platform.db --output data/evaluations/p45-rollout.json`，以及平台内的受登录保护接口 `GET /api/agent-rollout` 和运行详情中的“智能发布”页。它们独立汇总任务路由（12 条）、知识证据（40 条）与任务验收（35 条）固定集，并只读取 Run 元数据与验收事件判断 V1/V2 Shadow 样本、完成率、有效反馈、工具/模型调用、验收失败率、P95 时延和发布建议；不读取对话或资料正文。平台接口只统计当前用户的 Run，避免跨用户泄漏。端到端回归已验证：启用四个 Shadow 模式后，新对话会冻结 TaskFrame 并作为 V2 Shadow Run 被发布报告计入。候选完成率低于 90%、低于 V1 基线或验收失败率超过 10% 时建议 `rollback`；时延超过 V1 两倍、或具备足够评价样本后有帮助率回退时维持 `shadow`。固定集全部通过，但当前本地库无 V2 Shadow 样本，因此建议保持 `shadow`，不会自动进入管理员灰度。回滚为设置 `AGENT_INTELLIGENCE_V2=false`，无需数据库回滚，历史 Run 保留原上下文。P44 的检索策略候选发布/回滚继续独立运行，P45 不会绕过其门槛。

执行补充 P45-7e（2026-07-20）：用户已明确授权本机跳过真实样本门槛并进入 Active。当前发布报告固定集为任务路由 12/12、证据充分性 40/40、任务验收 35/35；运行元数据中仅有 1 条 V2 样本，统计状态仍为 `insufficient`，因此报告建议保持 Shadow 观察，但不会自动修改用户选择的 Active 配置。回滚开关继续为 `AGENT_INTELLIGENCE_V2=false`；后续只累积无正文的运行、时延、预算、工具和反馈指标。

新增五组独立评测，禁止用一个综合分数掩盖问题：

1. 任务理解：目标、交付物、约束、澄清判断。
2. 证据充分性：应继续检索、应停止检索、应澄清、应带限制回答。
3. 工具决策：工具选择、参数、调用有效性、重复/无效调用。
4. 任务验收：交付物覆盖、无依据结论、工具执行真实性、引用支持。
5. 端到端任务成功：固定真实风格任务的完成率、时延、成本和用户反馈。

实施项：

- 【】P45-7a：先冻结 V1 基线；每个评测样例标注预期状态与证据，不以模型自评作为唯一真值。
- 【】P45-7b：V2 先运行 Shadow，对同一任务记录 V1 实际行为和 V2 建议，不重复执行有成本或副作用的工具。
- 【】P45-7c：发布门槛：任务成功率和严重证据错误不得回退；无依据结论率下降；工具有效率提升；P95 时延、平均模型调用数和 token 成本保持在批准预算内；所有权限、安全、确认和恢复测试必须通过。
- 【】P45-7d：按 `orchestrator_version`、Planner/Verifier 版本和模型角色聚合“有帮助 / 没帮助”、引用评价、工具成功率和澄清完成率；真实样本不足时只显示观察，不宣称提升。
- 【】P45-7e：发布顺序固定为开发开关 → Shadow → 管理员账户 → 小比例本地任务 → 默认开启；任一阶段可切回 V1，历史 Run 保留原版本。

验收：提供一键运行的固定评测命令和机器可读报告；候选未过门槛时自动给出 `rollback`；关闭开关无需数据库回滚即可恢复 V1。

#### P45-8：Terra 实施批次与交接约束

为降低一次性重构风险，使用以下顺序，每个批次单独开发、测试、记录和提交：

1. **批次 A：P45-0/P45-1**。只做基线、Task Frame、模型辅助规划和 Shadow 事件，不改变最终回答与工具行为。
2. **批次 B：P45-2（仅知识库）**。实现 Evidence Ledger、知识证据覆盖和受限查询改写；继续使用 V1 执行循环。
3. **批次 C：P45-3（只读工具）**。接入状态化循环，先覆盖工作区检索/读取和网页只读工具；写操作仍走原确认链路。
4. **批次 D：P45-4/P45-5**。接入任务验收器和模型角色路由，比较质量、时延与成本。
5. **批次 E：P45-6**。完成对话展示、澄清恢复与反馈原因，不提前于后端契约。
6. **批次 F：P45-7**。Shadow 对比、管理员灰度、门槛发布与回滚演练。

### 后续统一执行路线图（2026-07-20）

以下将已规划但尚未开始、等待外部条件，以及 P45 的剩余收口统一管理。完成项不再重复排入执行队列；每项开始前仍需补齐定向验收与回归范围。

| 顺序 | 工作项 | 状态 | 进入条件与交付边界 |
| --- | --- | --- | --- |
| 1 | P39-6 项目空间知识库安全回归 | 待开始 | 补齐跨项目隔离、资料删除权限、历史引用脱敏和端到端回归；不改变通用资料全局可检索的既定规则。 |
| 2 | P19b 风险工具审批 | 待开始 | 将写入、本地高风险和特权工具改为可持久化审批节点；只展示安全的变更摘要，批准后从同一 Run 恢复。 |
| 3 | P45 剩余收口：统一证据与产物验收 | 待开始 | 细分状态见下表；将用户事实、记忆和工具观察完整纳入 Evidence Ledger；工具结果后重评估缺口；为代码/文件交付补充严格的实际产物与测试验证。 |
| 4 | P45 Active 运行观察与回滚演练 | 进行中 | 本机已在用户授权下进入 Active；继续收集不含正文的 Run 元数据，验证预算、权限、回滚与异常恢复。真实样本不足时只观察，不宣称质量提升。 |
| 5 | P35 真实评价质量基线 | 等待样本 | 累积真实的回答/引用评价后，生成有统计意义的准确率、帮助率和版本对比；不以检索命中替代人工质量。 |
| 6 | P36 图片/OCR 与复杂内容链路 | 条件待开始 | 先配置并验证视觉模型或本地 OCR，再接入图片解析、权限控制、引用链和回归；PDF/XLSX 已完成，不重复建设。 |
| 7 | P38 / LangGraph 复评 | 条件待开始 | 仅当出现并行分支、跨进程等待、复杂循环或后台 Worker 的真实案例时，与现有 SQLite 运行时做同案例量化比较；未触发前不引入框架。 |
| 8 | 部署环境真实验收 | 等待部署条件 | 在目标环境由部署者完成真实模型/外部服务连通、生产管理员和恢复演练；本地不读取、记录或暴露真实密钥。 |

执行原则：先完成 1 的安全回归，再完成 2 的写操作审批边界；随后收口 3，并持续执行 4。第 5 至第 8 项均依赖真实样本、能力配置或部署环境，不因计划存在而绕过其进入条件。

#### P45 细分任务台账

| 子任务 | 当前状态 | 后续动作 |
| --- | --- | --- |
| P45-2a | 部分完成 | 知识库、网页和工作区来源已进入 Evidence Ledger；补齐用户明确事实、长期记忆和工具观察的统一元数据入口。 |
| P45-2b | 已完成 | 保持确定性覆盖判断加模型受限判断，纳入回归即可。 |
| P45-2c | 已完成 | 保持最多两次、隔离受限的改写查询。 |
| P45-2d | 已完成 | 保持 `retrieve_more / clarify / answer_with_limits` 终态边界。 |
| P45-2e | 已完成 | 保持“有结果不等于证据充分”的语义与兼容字段。 |
| P45-3a | 已完成 | 显式状态机与非法迁移防护已具备。 |
| P45-3b | 已完成 | 结构化 `next_action` 和平台二次权限校验已具备。 |
| P45-3c | 部分完成 | 已防止无新证据的重复调用；补齐每次工具观察写入账本后触发的统一缺口重评估。 |
| P45-3d | 已完成 | quick / standard / deep 的调用与工具预算已实现。 |
| P45-3e | 已完成 | 预算耗尽会进入带限制终态。 |
| P45-3f | 已完成 | Run 已按 Task Frame 生成动态步骤，并记录开始、完成、失败、跳过与等待。 |
| P45-4a、4c、4d、4e | 已完成 | 任务验收器、结构化结果、一次修订限制与不可用边界已具备。 |
| P45-4b | 部分完成 | 普通、知识、分析、方案已覆盖；补齐代码变更的关联文件/测试验证，以及文件任务的实际产物存在性验证。 |
| P45-5a ～ 5e | 已完成 | 角色、能力、回退、预算和 reasoning 信号安全边界已具备。 |
| P45-6a ～ 6e | 已完成 | 对话审计层、澄清卡、工具摘要与独立反馈已具备。 |
| P45-7a ～ 7d | 已完成 | 固定评测、Shadow/Active 元数据、质量门和发布报告已具备。 |
| P45-7e | 运行观察中 | 用户已授权本机进入 Active；真实样本门槛暂缓，不自动扩大范围，保留一键回退。 |

每批次必须执行：

- 先阅读本节、`docs/backend-architecture.md`、`docs/frontend-contract.md`、`docs/decision-quality.md` 和相关模块测试；不得绕过现有服务边界把逻辑回流到 HTTP Handler。
- 先新增契约和单元测试，再接入主链路；任何模型输出都必须经过 Schema 校验和确定性权限复核。
- 使用 `apply_patch` 编辑；保留工作区中无关的用户文件；不得提交 `data/`、`node_modules/`、素材图片或其他项目目录。
- 至少运行新增单元测试、路由/知识/决策固定评测、服务端关键回归、`scripts/check-frontend.sh` 和 `git diff --check`；需要本机端口时按权限流程运行。
- 更新本 Plan 的批次状态、实际指标、失败样例、开关默认值和回滚结果后再提交；一个提交只包含一个批次的可回滚变更。
- 若固定评测回退、权限隔离失败、循环无终态、P95 时延或模型调用超过预算，不继续下一批次；先回滚本批行为或保持 Shadow。

Terra 首个开发任务：只执行“批次 A”。不要同时实现 Evidence Ledger 或替换 `SingleAgentLoop`。完成 Task Frame 契约、Shadow Planner、固定评测、Run 事件和开关后停止，提交结果并等待下一阶段确认。
