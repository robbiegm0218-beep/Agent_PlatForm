# P47 第二轮远程试用观察记录

观察日期：2026-07-28
范围：生产 Docker 实例的匿名化运行元数据，不读取用户 ID、对话正文、知识正文、产物内容、反馈备注或凭据。

## 结论

P47-4 首个远程观察周期通过运行稳定性验收。生产实例状态为 `ok`，无运维告警；47 个 Run 的失败率和模型错误率均为 0。受邀用户累计 31 个 Run，31 个完成、0 个失败。

线上反馈显示回答使用证据的质量仍需观察：回答评价 17 条有帮助、6 条无帮助；逐引用评价 15 条正确、11 条不正确，其中 9 条为 `answer_misused`、2 条为 `wrong_document`。当前活动检索策略的固定离线评测 20/20 通过，召回、Top1、空结果和相邻片段准确率均为 100%，因此不执行检索策略回滚。后续优先观察模型如何使用已检索证据，不根据本次结果自动修改线上策略。

## 运行与容量

| 指标 | 结果 |
| --- | ---: |
| 实例状态 | `ok`，无告警 |
| 全部 Run | 47 |
| 全部 Run 失败率 | 0% |
| P95 | 26.87 秒 |
| 模型请求 / 模型错误 | 47 / 0 |
| 当日 Token 估算 | 761 |
| 当月 Token 估算 | 6,493 |
| 受邀用户 Run | 31 |
| 受邀用户完成 / 失败 | 31 / 0 |
| 受邀用户 Token 估算 | 5,009 |
| 可用磁盘 | 457,121,148,928 字节 |
| 最近快照年龄 | 约 48 分钟 |

## 反馈与访问

| 指标 | 结果 |
| --- | ---: |
| 回答有帮助 / 无帮助 | 17 / 6 |
| 逐引用正确 / 不正确 | 15 / 11 |
| `answer_misused` | 9 |
| `wrong_document` | 2 |
| 最近 24 小时登录成功 / 失败 | 1 / 2 |

登录失败没有造成账号锁定或服务告警，之后已有一次成功远程登录。

## 检索策略复核

- 当前活动策略：`candidate-fe049d57fa47`
- 固定离线用例：20
- `recall@4`：100%
- Top1：100%
- 空结果准确率：100%
- 相邻片段准确率：100%
- 失败用例：0
- 决策：不回滚；保持人工发布和回滚边界。

## 后续

1. P47-3 按用户要求暂停，不计为完成。
2. P47-4 完成后转入运维 Runbook 的每周元数据检查。
3. 扩大试用前继续观察 `answer_misused`；如固定评测或线上稳定性出现回归，先回滚或修复再扩大范围。
